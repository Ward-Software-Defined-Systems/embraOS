# embraOS Change Log

Chronological merge log for `main`, anchored on tagged releases. Each entry's headline summary is pulled from the corresponding sprint section in [../ARCHITECTURE.md](../ARCHITECTURE.md) (local doc, gitignored) — that file remains canonical for design narrative; this file is the navigable release index.

**Last updated:** 2026-07-19.

---

## Unreleased — `main` since v0.5.0-phase1

`main` advanced past `v0.5.0-phase1` with thirty-nine merged post-Sprint-5 increments through 2026-07-19 — the latest is the **named-volume Buildroot flow for the Mac Docker builds** (the OrbStack EMFILE verdict: bind-mount Buildroot trees exhaust the macOS file provider's per-inode fd pool, VM-native named volumes don't — operator-verified on the failing M2; both Mac guides now default to it, with a stale-tree QEMU warning; closing entry of this section), preceded by the **build-script fd preflight + `JOBS` parity** (guest-side hardening from the same investigation: soft-fd self-raise + an fd/parallelism report before Step 4 in both image scripts, and `build-image.sh` finally honoring `JOBS`), preceded by the repo's **first GitHub Actions CI** (four jobs: storage-pin guards, the Trunk→WASM frontend build, workspace tests + the wardsondb lint ratchet, and the Bootlin-toolchain musl cross-build; a complement to the local image/QEMU pipeline, never its replacement), preceded by **WardSONDB vendored into the workspace as `crates/wardsondb`** (byte-faithful in-tree fork of upstream `main` @ `42fea43` — 12th workspace member, the sibling-repo build step removed, upstream's regression-tested storage pins preserved), preceded by **`/stop`, the out-of-band operator interrupt for stuck turns** (a new unary `StopTurn` RPC + a stop-generation watch that aborts in-flight streams, tool dispatches, and learning turns; the provider pumps now exit on receiver-loss, which is what actually severs a looping local model's connection; TUI Esc + mobile ■ button), preceded by the **duplicate-session-name fix** (`/new` and `create()` refuse existing names — the unguarded create appended shadowed duplicate docs — plus a boot-time dedupe healing existing duplicates; caught by William's bulk delete testing), preceded by **`/sessions delete` + `restore`, guided soft deletion with a TTL grace period** (the intelligence summarizes the session, asks the operator's reason, preserves memories to the KG, then the brain soft-deletes on the model's READY marker; 7-day WardSONDB TTL reap + boot ghost-sweep + restore), preceded by the **WardSONDB code-review compatibility wave** (boot-ensured single-field `idx_edge_source_id` restoring the KG traversal source arm's index under WardSONDB's F2 planner fix, cursor-adaptive dump/orphan pagination, and the `embra.dbverbose=1` opt-in), preceded by the **vetted-roster narrowing to local-only** (the local-inference list 2 → 1: `DeepSeek-v4-Pro:cloud` removed as Ollama-hosted, not local; the MoE active-parameter rationale stays as general guidance), preceded by the **session-UX fix trio** (real session turn counts + the resume-briefing idle gate + the no-session prompt guard, plus the `/sessions` last-active timezone render, from William's mobile-web testing), the **recommended-models revision** (the vetted local-inference list narrows 4 → 2 — `DeepSeek-v4-Pro:cloud` + `Qwen3.6-27b` — with the MoE active-parameter rationale), the **cross-provider session-block message fix** (the block now spells out the `/close` → `/provider` → `/switch` navigation), the **Anthropic transient-error retry** (exponential backoff on 408/409/429/5xx incl. 529 overloaded; same-branch follow-up), **llm-provider-optimizations** (Fable 5 selectable + the Opus 4.8 default + refusal-as-error + streaming-client timeouts + `/effort`, plus the same-branch second wave: the `set_name` rename tool with its Phase-2 learning sync, and expression-panel scroll), the **embra-web console promotion to stable** (docs-only; the default UI since 2026-05-16 sheds its experimental designation), the **KG retrieval performance rework** (arm-split indexed traversal + NodeStore prefetch; the 5–8 min production `knowledge_query` → ~2 s; branch `knowledge-and-webui-fix`), **`knowledge_dump` + the `guardian_call` `data_file` bridge + the committed kg_scan example**, the **memory/KG search-window + console input-layout fix pair** with its `recall unpromoted_only` and windowless-KG-maintenance follow-ups, **guardian self-authoring + the soul-spec replicant check**, the **xterm Canvas-renderer** web-terminal fix (`f9c28b8`), the **feedback-loop spec v2.2** steps-only reduction (`7e944c2`→`23bb058`), and the **embra-brain-broadcaster** fast-forward (`7f6c479`) that closed the three open embra-web-mobile bugs + added Opus 4.8 + chat-mobile tweaks. Counts after the increments: 95 tools (90 → 92 at the embra-guardian-v1 merge for the `guardian_call`/`guardian_list` meta-tools; → 93 with `guardian_propose`, the brain self-authoring gate; → 94 with `knowledge_dump`, the KG JSONL export; → 95 with `set_name`, the operator-agreed rename); embra-brain 599 tests / embra-console 39 / embra-guardian 53 / embra-web 15 (12 new in `6ca54e8`, +3 in `541cc69`→`bb15a61`) / wardsondb 224 (vendored with the crate: 37 unit + 184 integration + 3 deadlock-stress); workspace 9 → 12 members (the 12th is the vendored `crates/wardsondb`). Workspace version: `0.5.0-phase1` → `0.6.0-phase1` at the 2026-06-13 Zenodo release (`c17e852`, tag `v0.6.0-phase1`), → `0.7.0-phase1` at the 2026-07-05 release (tag `v0.7.0-phase1`), → `0.8.0-phase1` at the 2026-07-18 release (tag `v0.8.0-phase1`).

**`4f5db06` — structured + precedence soul/identity/profile rendering (2026-05-15).**

**embra-web web console (8 commits, 2026-05-16, fast-forward to `acb241e`).** Self-hosted HTTPS console on `:3345` that wraps the real serial TUI in xterm.js over a PTY→WebSocket bridge. Now the **default UI** (`run-qemu.sh` sets kernel cmdline `embra.web=1`; `EMBRA_TUI=1` → stable serial-TUI fallback, no rebuild). **Experimental.** 2 new crates (`embra-web` workspace member + workspace-excluded `embra-web-ui` Leptos/WASM); workspace 9 → 10 members; no proto/schema/registry change; +0 tests.

**embra-guardian-v1 (fast-forward 2026-05-17).** Operator-authored dynamic WASM tools pulled forward from Phase 2: `syn`-validated Rust module → in-OS prebaked Rust toolchain → `wasm32` → `wasmtime` sandbox, capability-broker host imports including `http_get` and Brave `web_search`, `guardian_call`/`guardian_list` meta-tool gateway, new `embra-guardian` crate #11. Followed by the README→OS-manual restructure chain (`e3f5643`→`f8cad9c`, including `24f66b4` curl apt-dep and `f8cad9c` Guardian natural-language phrasing).

**`f6a684a` — aarch64/Apple-Silicon dual-arch Buildroot parameterization (2026-05-19).** One committed Buildroot tree builds both x86_64 and aarch64. Both arches QEMU-verified by William 2026-05-19.

**`c33047f`→`bac1d08` — Intel Mac build guide + Docker-helper apt-get sync (2026-05-20).** New `docs/INTEL-MAC-BUILD.md` plus minimal-scope Docker-helper apt-get sync in `scripts/build-image.sh`. Full end-to-end verified on Intel MacBook Pro 16" (HVF accel, Buildroot 2026.02.1, fjall) including Config Wizard, soul-formation, Guardian dynamic-tool round-trip, EMBRA_TUI=1 serial mode, and reboot soul-verify.

**`b4e94d1`→`7a75a25` — embra-web modal focus return + embra-brain session-resume briefing (2026-05-21).** Modal focus-return Effect at App root watches palette/modal/editor state and calls `term::focus()` on any-open→all-closed. Session-resume briefing dispatch on `SessionAttach` AND the `/switch` slash command (the latter added in `7a75a25` after an operator reported `/switch` skipped the briefing).

**`981818c` — README header callouts (2026-05-21).** Two operator-facing callouts: local-inference model vetting (DeepSeek-v4-Pro:cloud + three Qwen variants confirmed full-toolset-capable) and "memory & knowledge graph today — operator-driven, by conversation" (manual today, automation near-term; `/feedback-loop` is soul/identity realignment, not memory promotion).

**Feedback-loop spec tightened to v2.1 operational backbone (2026-05-24).** `crates/embra-brain/src/brain/feedback_loop_spec_v2.md` rewritten 435 → 274 lines: cut header metadata, Overview, When-to-Run, inline rationale paragraphs, Token Usage Tracking, the Operational Practices list, First Iteration Results, Future Iterations, the duplicate Evaluation Dimensions section, and the closing quote. The 5-step backbone (Gather/Evaluate/Reconcile/Execute/Record) with sub-steps, tool invocations, severity framework, evaluation-dimensions table, decision framework, action definitions, governance boundary, auto-execute tool table, and knowledge-promotion blocks is preserved. `include_str!` path unchanged (`grpc_service.rs:3378`); brain rebuild required for runtime effect because `include_str!` is compile-time. No proto/schema/tool-count/test-count change. Motivation: the spec is pasted verbatim into the synthetic user message every `/feedback-loop` invocation, so every line of narrative cost tokens at runtime.

**`88a2cfb` — auto Max-thinking dispatch for DeepSeek-v4-Pro on Ollama (2026-05-24).** OpenAI-compat provider now sends `reasoning_effort: "max"` when the configured Ollama model contains `deepseek-v4-pro` (case-insensitive substring; matches both `deepseek-v4-pro` and `deepseek-v4-pro:cloud`) — engages the model's deepest of three thinking modes per DeepSeek's primary docs (`api-docs.deepseek.com/guides/thinking_mode` documents `reasoning_effort: "high/max"`). Refactor: `model_supports_reasoning_effort(&str) -> bool` → `reasoning_effort_for_model(&str) -> Option<&'static str>` so the route table carries the effort value directly; existing reasoning models (`deepseek-r1`/`r2`, OpenAI o-series, `gpt-oss*`, `-thinking` variants) continue to receive `"high"` unchanged. +1 embra-brain test (`reasoning_effort_for_model_routes_deepseek_v4_pro_to_max`) → 439. README callout added in the vetted-models block. Empirical Max-thinking response-signature confirmation in QEMU still pending — only Learning-Mode boot reach verified at commit time; if `"max"` is rejected at `/v1/chat/completions` the fix is a one-character return-value change to `"high"`.

**`0739ce4` — `docs/RECOMMENDED-LOCAL-MODELS.md` refresh (2026-05-25).** Adds an "Ollama Cloud (no local hardware required)" tier featuring `deepseek-v4-pro:cloud` with its three thinking modes and the auto-Max-thinking dispatch landed in `88a2cfb`. Appends a Cloud-bypass note to the Ollama Server Configuration block clarifying that Cloud models bypass `OLLAMA_CONTEXT_LENGTH`/`OLLAMA_FLASH_ATTENTION`/`OLLAMA_KV_CACHE_TYPE` (host-side knobs only) and that `num_ctx` is not settable via the OpenAI-compat layer per `ollama#7063` regardless of mode. Refreshes a stale "Sprint 5 smoke-testing" status reference to "Phase 1 stable since v0.5.0-phase1". 1 file, +18 / −3. No code touched.

**`0b6c81a` — SSH-tools' redundant 10 KB output cap removed (2026-05-25).** `ssh_remote_admin` (`crates/embra-brain/src/tools/security.rs:507–517`) and `ssh_session_exec` (`:711–725`) carried their own 10 KB stdout/stderr truncation inside the tool handler, applied *before* the global 2 MiB `MAX_TOOL_RESULT_SIZE` cap in `tools::registry::dispatch::apply_max_size`. Pre-2-MiB-cap leftover that pre-emptively killed legitimate SSH output (`journalctl --no-pager -n 500`, `dmesg`, log tails, package lists routinely >10 KB) and contradicted CLAUDE.md's documented ceiling. Operator-reported during the 88a2cfb test session: SSH commands silently truncated at ~10 KB while the documented cap was 2 MiB. Deleted both per-tool 10 KB blocks + the local `truncate_str` helper + the "10KB output truncation" wording from `ssh_session_exec`'s `#[embra_tool(description)]` + `docs/TOOL-REFERENCE.md` rows 133 + 135. SSH now inherits the global 2 MiB ceiling like every other tool (`file_read` precedent — `FILE_READ_MAX = 2_097_152` is already aligned to the same value). 2 files, +6 / −40. embra-brain test count unchanged at 439 (no test asserted the 10 KB cap). Tool count, schema, proto, RFC 1918 enforcement, and the existing 30 s SSH-exec inner timeout all unchanged. Plan: `~/.claude/plans/a-while-back-in-compiled-sunbeam.md`.

**`92bc96a` — Global tool-execution timeout + operator-facing tool-execution indicator (2026-05-25).** Two coordinated additions to `tools::registry::dispatch`, bundled because they share the operator-context that motivated both (William's same SSH-testing session). **(1) Global timeout** — architectural mirror of `MAX_TOOL_RESULT_SIZE`. New `MAX_TOOL_DURATION = Duration::from_secs(600)` (10 min) bounds every handler future uniformly; expiry surfaces `DispatchError::Timeout { tool, limit_secs }` to the grpc_service tool-loop, which maps it to an `is_error: true` tool_result with a model-facing explanation ("Tool 'X' exceeded the 600s global execution timeout. The command may still be running on the remote/host but its result will not be visible to you. Consider breaking the work into smaller chunks or backgrounding it."). The timeout wrap is lifted into `pub(crate) enforce_timeout<F>` so the behavior is unit-testable without standing up a live `DispatchContext` against the 92-tool `REGISTRY` — `+3` embra-brain tests (`passes-through-fast-ok` / `passes-through-handler-error` / `returns-timeout-when-exceeded`) bring the count `439 → 442`. The inner 30 s wrappers in `ssh_remote_admin` (`security.rs:473`) and `ssh_session_exec` (`:670`) are removed since they were the operational bottleneck (`apt upgrade`, `make`, `rsync`, multi-package log scrapes routinely exceed 30 s); their "command timed out after 30 seconds" error arms go away with the timeout-tuple shape; the match becomes `Ok(output)` / `Err(e)` directly. **Kept** (atomic sub-op or fast-fail handshake, all <600 s): `port_scan` per-port 1–2 s, `ssh_session_start` 15 s open + 10 s probe, `ssh_session_end` 5 s close, `gh_clone` 120 s. **Known pre-existing limitation:** `tokio::process::Command::output()` futures don't kill their child on drop; a subprocess-spawning tool that hits the global timeout leaks the child until it finishes naturally — the same leak existed under the per-tool 30 s wrappers, the window is just wider now. Mitigation requires `.kill_on_drop(true)` on every `Command` builder; deferred. **(2) Tool-execution indicator** — operator-facing indicator showing `"<name> is running <tool> (Ns)..."` while a tool is mid-dispatch, replacing the generic `"thinking..."` that left operators wondering whether anything was happening during long tool calls. Rides on the existing Thinking proto signal via one additive field (`string current_tool = 3` on `ThinkingState`) — proto3 additive, no WardSONDB schema bump, no tool-count change. The brain emits `Thinking{current_tool: name}` immediately before `dispatch` and `Thinking{current_tool: ""}` immediately after (regardless of outcome — never lingers on a finished tool). embra-console's existing `AppState` gains `current_tool: Option<String>` + `current_tool_started: Option<Instant>`; the elapsed clock resets only on a *changed* tool name, so duplicate signals during a single dispatch don't restart the displayed "(Ns)". `ui.rs`'s existing thinking-indicator branch picks the new label format when `current_tool` is set; the existing dots animation, color, and italic styling are preserved. embra-web inherits automatically (xterm.js wraps the same embra-console TUI over a PTY → WebSocket bridge). 10 files, +180 / −57. Tool count 92, schema v12, proto wire format additively compatible. Workspace tests green: embra-brain 442 / embra-console 21 / embra-guardian 44 unit + 4 e2e + 6 html_text. CLAUDE.md `Invariants & Gotchas` gains a sibling line under the existing tool-result-size cap entry documenting both the time cap and the subprocess-leak limitation (CLAUDE.md is gitignored — local-only). E2E QEMU verification deferred to William on the bug-source instance. Plan: `~/.claude/plans/a-while-back-in-compiled-sunbeam.md` (same plan file as `0b6c81a` — grew the timeout + indicator sections in conversation).

**`0f9d286` — Web-UI font-size CSS variable system + conservative bump (2026-05-25).** `crates/embra-web-ui/assets/app.css` previously hardcoded font-sizes across 21 selectors with 6 distinct pixel values (11 / 12 / 13 / 14 / 15 / 19 px), collapsing the visual hierarchy into 1-px gaps that read as dense on 1080p screens. xterm.js (`crates/embra-web-ui/assets/embra-term.js:67`) independently set `fontSize: 14`, one pixel below its own 15 px default. Five `:root` CSS variables now serve as a single source of truth: `--text-xs: 13px` (sidebar headers, pills, badges, codes, hints, labels — replaces the dense 11–12 px cluster across 12 uses); `--text-sm: 14px` (body baseline, command buttons, banner, editor, modal inputs — replaces 13–14 px); `--text-md: 16px` (modal titles, palette search — was 15); `--text-lg: 20px` (brand wordmark — was 19); `--term-fs: 15px` (xterm.js terminal — was 14, restores its own native default). All 21 hardcoded `font-size: Npx` declarations + 1 `font:` shorthand (`.modal .ml-input`) now reference the variables; future size changes become 1-line edits instead of a 22-site sweep. `embra-term.js` reads `--term-fs` at init via `getComputedStyle(document.documentElement).getPropertyValue("--term-fs")` (falls back to 15 if parse fails) so `app.css` is the single source of truth for the size hierarchy across both surfaces. Conservative bump — most secondary-text sites +1 px (the dense 12-px cluster gets the most breathing room), modal titles +1, terminal +1. 2 files, +39 / −22. **No Rust touched, no proto, no schema, no tool count.** Trunk build clean. embra-console (serial-TUI fallback, `EMBRA_TUI=1`) unaffected — host-terminal-controlled font.

**`6ca54e8` — CPU/MEM/LOAD meter pills in the embra-web topbar status bar (2026-05-25).** The `:3345` HTTPS console's topbar already rendered five service-health pills (`wardsondb`, `embra-trustd`, `embra-apid`, `embra-brain`, `embra-web`) from `GET /api/status` (`crates/embra-web/src/status.rs:58`) on a 5 s `gloo-timers` poll in the Leptos frontend (`crates/embra-web-ui/src/status.rs:26-39`), but had no system-resource visibility — a memory-pressured WardSONDB or a runaway tool loop pinning CPU looked visually identical to healthy idle until something fell over. New `crates/embra-web/src/metrics.rs` adds pure `(&str) -> Option<T>` parsers for `/proc/stat` (aggregate `cpu ` line; idle = idle + iowait per Linux convention; total = sum of all columns), `/proc/meminfo` (MemTotal + MemAvailable in kB → bytes), and `/proc/loadavg`, plus a `compute_cpu_pct(prev, curr)` delta calculator that returns `None` on zero-delta or counter-wrap. The IO layer is Linux-gated via `#[cfg(target_os = "linux")]` with non-Linux stubs returning `None` so `cargo check` from a macOS dev host stays green; no new crate dependency, matches the existing `/proc` precedent at `crates/embra-brain/src/proactive/mod.rs:206-218`. `AppState` (`crates/embra-web/src/state.rs:6-10`) gains `cpu_snap: Arc<Mutex<Option<CpuSnapshot>>>`; `std::sync::Mutex` is safe because the critical section is parse-and-swap, never crosses `.await`, and contention is structurally zero (one handler per 5 s poll). `api_status` extracts `State<AppState>`, samples CPU/MEM/LOAD on every call, and attaches an additive `"system"` JSON field (`Value::Null` when nothing useful is available so the frontend hides the meters cleanly): `cpu_pct` (null on first poll — no baseline), `cpu_count` (`std::thread::available_parallelism()`, used by the frontend to grade LOAD relative to capacity), `mem_total_bytes` / `mem_used_bytes` / `mem_pct`, and `load1` / `load5` / `load15`. Frontend `StatusData` (`crates/embra-web-ui/src/status.rs:7-23`) gains `system: Option<SystemMetrics>` with the matching struct, all fields `#[serde(default)]` for backward-compatible polling against an older `/api/status`. Three new pill renders appear in the existing `.pills` flex region after the services loop (`crates/embra-web-ui/src/app.rs:269-278`), sharing the same 5 s polling loop — no new endpoint, no new cadence. CPU and MEM use a bar+number layout (32 × 8 px fill bar + label + percent inside a `.pill.meter` variant); LOAD uses a dot+value layout (`.pill.load`) since load average has no natural ceiling. Severity classes `good`/`warn`/`danger` trigger at 70 % / 90 % for utilization, and at 1× / 2× `cpu_count` for load; CSS reuses the existing `--good` (`#43d17a`) / `--warn` (`#f5a623`) / `--bad` (`#e5533a`) / `--panel-2` / `--line` tokens — **no new color additions**. CPU pill is hidden on the first poll (no baseline yet — appears ~10 s after page load); MEM and LOAD appear after the first successful poll (~5 s). Tooltips: MEM shows `"15.2 GB / 16.0 GB used"`, LOAD shows `"1m 0.42  5m 0.51  15m 0.48  (4 cores)"`. **+12 embra-web unit tests** in `metrics::tests` (parser fixtures valid + malformed + `compute_cpu_pct` edges including zero-delta, counter-wrap, fully-idle interval, and the basic 30 % case) — the first unit tests in this crate. Workspace test suite green: embra-brain 442 / embra-console 21 / embra-guardian 44 lib + 4 e2e + 6 html_text / **embra-web 12 (new)**. Trunk wasm build of `embra-web-ui` clean. 7 files, +436 / −2 (1 new `metrics.rs`, 3 backend edits, 3 frontend edits). **Strictly above embra-apid** — no proto change, no WardSONDB schema bump (still v12), no `build_tools_snapshot` change (still 92 tools), no system-prompt mutation; serial TUI path (`EMBRA_TUI=1 ./scripts/run-qemu.sh`) untouched (entire embra-web stack skipped in that mode). Per-service CPU / MEM (would require routing PIDs through the unimplemented `apid::SystemHealth` RPC at `embra-apid/proto/apid.proto:73-78`) deliberately deferred — system-wide is the right first cut and doesn't need a backbone change. QEMU smoke-test deferred to operator. Plan: `~/.claude/plans/the-status-bar-for-snappy-babbage.md`.

**`541cc69`→`bb15a61` — DATA + STATE meter pills in the embra-web topbar status bar (2026-05-25).** Two filesystem-utilization meters join the CPU/MEM/LOAD trio shipped in `6ca54e8`, covering the two ext4 partitions where free space actually moves under operational load: DATA (`/embra/data` — WardSONDB, workspace, memory, sessions, knowledge graph) and STATE (`/embra/state` — PKI, soul hash, API keys, config files). Rootfs (SquashFS, always 100 % by design) and boot (vfat, fixed at install) are excluded as useless signals. **Backend.** New `crates/embra-web/src/metrics.rs::read_disk_info(path: &str) -> Option<DiskInfo>` calls `libc::statvfs` once per invocation (single syscall, microseconds); `f_bavail × f_frsize` is used rather than `f_bfree × f_frsize` to match `df`'s "available to an unprivileged process" semantic — reserved blocks aren't "free" from the operator's perspective. Linux-gated via `#[cfg(target_os = "linux")]` with a non-Linux stub returning `None`, matching the existing `/proc`-reads pattern from `6ca54e8`. `libc = "0.2"` added to `crates/embra-web/Cargo.toml` — already in the workspace dep tree via `embrad` / `embra-brain` / `embra-console`, so the marginal cost is one line. `collect_system_metrics` (`crates/embra-web/src/status.rs`) reads both mountpoints (paths from the embrad invariants in `crates/embrad/src/config.rs:6-7`) and attaches per-partition `data_pct` / `state_pct` percents plus per-partition `data_total_bytes` / `data_used_bytes` / `state_total_bytes` / `state_used_bytes` to the additive `system` JSON field. **Frontend.** `SystemMetrics` (`crates/embra-web-ui/src/status.rs`) extends with the matching `Option<...>` fields. The topbar render inserts two `.pill.meter` pills (DATA + STATE) between MEM and LOAD — final order is **CPU / MEM / DATA / STATE / LOAD** (four bar+% meters grouped, LOAD's dot+value variant trailing). Each pill grades on its own percent with the same 70 / 90 % severity thresholds as CPU/MEM (more meaningful for disk since fill is slow and one-way). Tooltips use a new `fs_tooltip(used, total, mount)` helper matching the MEM pill's `"X / Y used on /path"` shape exactly; a `fmt_compact_bytes` helper picks GB / MB / B so STATE (typically tens of MB) doesn't render as "0.0 GB". CSS reuses `.pill.meter` and the existing `--good` / `--warn` / `--bad` tokens — **no new tokens, no new pill variant**. **Tests.** +3 embra-web unit tests in `metrics::tests` (`disk_used_pct_basic` at 25 % of 64 GB, `disk_used_pct_zero_total_is_none`, `disk_used_pct_full`); the statvfs FFI itself isn't unit-testable but the math is. embra-web 12 → **15**. Workspace test suite green: embra-brain 442 / embra-console 21 / embra-guardian 44 lib + 4 e2e + 6 html_text / embra-web 15. Trunk wasm build clean. **Same-session correction chain.** `541cc69` initially landed as a single DISK pill rolling up `disk_pct = max(data_pct, state_pct)` with a combined per-partition tooltip — William's "Fold STATE into the DISK" was meant as "render alongside as a peer pill", not "combine into one rollup". `bb15a61` (3 files, +36 / −46) split the single pill back into the two independent DATA + STATE pills before any operator saw the rollup form; `bb15a61` is the canonical shipped form. The fold-vs-peer interpretation gap is captured in a new feedback memory so the next ambiguous UX phrasing gets asked, not guessed. **Strictly above embra-apid** — no proto change, no WardSONDB schema bump (still v12), no `build_tools_snapshot` change (still 92 tools), no system-prompt mutation; serial TUI path (`EMBRA_TUI=1`) untouched. 9 files across both commits (6 in `541cc69` + 3 in `bb15a61`), net +145 / −50. QEMU smoke-test deferred to operator. Plan: `~/.claude/plans/the-status-bar-for-snappy-babbage.md` (extended in-conversation through both commits).

**embra-web-mobile (12 commits, 2026-05-28, fast-forward to `e04374a`).** New chat-style mobile UI mode for the embra-web HTTPS console, served by the same Leptos/WASM bundle as the desktop xterm.js wrapper but rendered conditionally at the root based on viewport width + a localStorage preference. The mobile path bypasses xterm.js + the ratatui TUI entirely — an initial responsive-CSS attempt (also on this branch, rolled back before merge) didn't survive iPhone testing because the ratatui grid expects ~80 cols and won't compress to ~36 on a 390 px viewport. The chat path talks to embra-apid's gRPC `Converse` stream directly through a new `/ws/chat` WebSocket bridge in `embra-web`. **Experimental** — bugs #1 (empty-looking Tool bubbles for silent-output tools like `file_write`) and #2 (reconnection-briefing replays on iOS Safari app-switch) remain open; bug #3 (desktop terminal shows stale mode after a mobile-driven `ModeTransition` because the brain emits state changes only on the originating stream) is architectural and queued for a follow-up branch alongside #1/#2.

**Backend additions (`crates/embra-web/`).** New `/ws/chat` Axum WebSocket handler (`ws_chat.rs`) opens its own tonic `Channel` to apid (`http://127.0.0.1:50000`, plaintext over localhost) per WS connection and bridges JSON↔proto in both directions through `chat_bridge.rs`. The bridge handles the `apid::ConversationResponse` → `brain::ConversationResponse` `bytes payload` double-wrap via `prost::Message::decode()` (same pattern embra-console already uses). The browser's wire protocol is a tagged JSON enum: client→server `msg{text}` / `slash{command,args}` / `attach{session}`; server→client `token` / `done` / `system` / `tool` / `thinking` / `mode` / `setup` / `reasoning` / `error` (mirrors the brain's `ConversationResponse` oneof + lowercases the enum-typed `kind` / `field_type` / mode names). New `GET /api/sessions` (`sessions.rs`) calls apid's `ListSessions` RPC and returns the parsed list as JSON for the sessions bottom sheet. `AppState` gains `apid_addr: String` (cloned from `WebConfig::apid_addr` at boot). All static asset responses now carry `Cache-Control: no-store` (`assets.rs`) to bust browser caches across OS image rebuilds — mobile Safari was happily serving stale cached `index.html` (and the SRI-pinned old WASM) across iterations during testing. Slash command parsing stays server-side — the brain owns the dispatcher; the chat client just sends `Slash{command, args}` as a typed variant, no client-side `/` parser.

**Frontend additions (`crates/embra-web-ui/`).** Root-level mode-switch in `main.rs` (`Root` component + `pick_chat_mode()`) detects viewport ≤ 768 px OR `localStorage.embra-mode == "chat"` and renders `chat::ChatApp` or the existing `app::App`. Two cross-mode toggle buttons — `↗ desktop` on the chat topbar and `↗ mobile` on the desktop topbar — both flip localStorage + reload. The new `chat.rs` module (~1300 lines) holds the entire chat surface: `ClientMsg`/`ServerMsg` wire enums mirroring `chat_bridge.rs`; a `Bubble` enum (User / Assistant / System / Tool / Setup); WS lifecycle with an mpsc-backed outbound channel + auto-reconnect with exponential backoff (1 s → 10 s cap); auto-scroll-to-bottom on new content; iOS keyboard auto-scroll via `on:focus`; `SetupOverlay` sticky form for in-flight wizard prompts (text input / option buttons / yes-no per `field_type`); expandable `Bubble::Tool` cards with pretty-printed JSON input + result (4 KB display cap); live-reasoning side sheet (drops `ReasoningDelta` shards into an aux signal per REASONING-STREAM-01, surfaces a pulsing 💭 pill in the topbar that opens the sheet on tap); sessions bottom sheet (fetches `/api/sessions`, renders the row matching the current `session_name` with an `.active` highlight + " · current" suffix); slash command picker bottom sheet (22 commands across 6 groups — Session / Identity / Provider / Setup / Guardian / Help — minus `/ml` and `/guardian-define` and the multi-step wizards which need their own multi-line editor surface); services bottom sheet (reuses the existing `/api/status` 5 s poll). The chat surface shares the desktop's brand palette + font hierarchy + the in-app confirm sheet for `term::takeover()` (replacing the prior native `window.confirm()` — a desktop-neutral win that carried forward from the rolled-back touch-optimized pass). New cargo deps: `futures` / `futures-util` / `gloo-net websocket` feature; web-sys features `Storage` / `Element` / `HtmlElement`.

**Four-phase trajectory; each phase = its own commit on the branch.**
- **Phase 1a** — `/ws/chat` backend skeleton (`4f09af2`). gRPC client wiring + JSON↔proto bridge + WS handler. Testable with `wscat`.
- **Phase 1b** — frontend chat scaffold (`b48d524`). Root mode-switch + chat topbar + scrollable bubble list + input bar + WS reconnect. Slash commands work via typed `Slash{}` variant; brain parses server-side.
- **Phase 2** — sessions sheet + slash command picker + services chip + the new `/api/sessions` endpoint (`222d061` backend, `3e483c3` frontend). Three bottom sheets share one `.sheet` CSS shell; topbar gets a consolidated health-chip combining WS state + services aggregate.
- **Phase 3a/b/c** — rich rendering: Tool cards expand on tap with pretty-printed JSON (`a05cd7c`); `ReasoningDelta` accumulator + side sheet via 💭 topbar pill (`1709a85`); `SetupPrompt` inline form overlay between scroll and input rendering text / selector / confirm widgets per `field_type` (`9f543d7`) — wizards like `/provider --setup` and learning-mode boot are now usable from the phone.
- **Phase 4** — polish (`8029a3a`). Desktop→mobile toggle, iOS keyboard focus auto-scroll, active-session highlight in the sessions sheet.

**Known bugs.** Bug #1 (Tool bubbles render as empty blocks for tools with empty `result`) — defensive fallbacks render `(no output)` / `(no input)` / `(tool)` placeholders (`6d59d83`), but operator testing post-merge confirms the empty-bubble shape can still appear; root cause unknown. Bug #2 (reconnection-briefing spam on iOS Safari app-switch) — `has_connected_once: RwSignal<bool>` filter that flips on first non-reconnection message so the genuine first-connect briefing displays fully (`2e8f20f`), but operator testing confirms briefings still replay; root cause unknown. Bug #3 (desktop terminal stuck on old mode after mobile completes learning) — the brain emits `ModeTransition` only on the stream that caused it; the embra-console PTY child's gRPC stream never sees the mobile-driven transition. Operator workaround: type `/mode` or `/status` in the desktop terminal to force a re-query. Real fix is a brain-side broadcaster — queued for a follow-up branch where #1 and #2 will also be re-investigated with targeted logging.

**Strictly above embra-apid; no architectural footprint elsewhere.** No proto change (Converse + ListSessions already existed). No WardSONDB schema bump (still v12). No `build_tools_snapshot` change (still 92 tools). No system-prompt mutation. embra-console (serial-TUI fallback, `EMBRA_TUI=1`) untouched; `/ws/terminal` PTY bridge co-exists with `/ws/chat`. Test counts unchanged: embra-brain 442 / embra-console 21 / embra-guardian 44 lib + 4 e2e + 6 html_text / embra-web 15. `embra-web-ui` is workspace-excluded (Leptos/WASM, wasm32 toolchain) — no workspace test impact. Trunk build clean; cargo workspace check clean. ~2420 insertions / 13 deletions across 16 files (4 new — `chat_bridge.rs`, `ws_chat.rs`, `sessions.rs`, `chat.rs`). Plan: `~/.claude/plans/embra-web-is-working-perfectly-twinkling-ullman.md`.

**embra-brain-broadcaster — the three open chat-mobile bugs + Opus 4.8 + chat-mobile tweaks (branch off `3e64072`, fast-forward to `7f6c479` 2026-05-29).** Resolves the three known embra-web-mobile bugs (#1 empty-looking Tool bubbles, #2 reconnection-briefing spam, #3 desktop stuck on Learning), adds Opus 4.8 as a selectable Anthropic model, and a set of operator-reported chat-mobile UI tweaks. **Strictly above embra-apid for the brain-side pieces; the chat-mobile pieces live in the workspace-excluded `embra-web-ui`.** No proto change, no WardSONDB schema bump (still v12), no `build_tools_snapshot` change (still 92 tools — `/model` is a slash command, not a tool), no system-prompt mutation. embra-brain 442 → **455** (+13: Opus-model resolver + parser + display, the shared `soul_sealed_mode_change` `ModeTransition` constructor, the wizard provider-label mapping, and `AnthropicProvider::with_model`); embra-console 21 / embra-guardian 52 / embra-web 15 unchanged. QEMU E2E (dual-client learning + wizard) verified by William.

**Bug #3 (desktop stuck on Learning) — brain-side onboarding-stage wake signal.** The real cause was deeper than the merge-time "the brain emits `ModeTransition` only on the originating stream" framing: each `converse` call spawns its own task (`grpc_service.rs:158`), and while the soul is unsealed *every* attached stream independently runs its own first-run wizard / `run_learning_loop`, blocked on `incoming.next()` with no global soul-sealed re-check (`grpc_service.rs:3673-3674`) and ignoring slash commands there (`:3757`). So when mobile (stream A) sealed the soul, desktop (stream B) was genuinely stuck inside its own learning loop — fanning a display event to B would update its pixels but leave its task in learning. Fix: a brain-global `tokio::sync::watch::Sender<i32>` (current `OperatingMode` stage) on `BrainGrpcService`, advanced by whichever stream drives onboarding (→ Learning at wizard completion, → Operational at soul seal). The other streams' wizard `select!` and `run_learning_loop`'s blocking recv become a `select!` over `incoming.next()` **and** `stage_rx.changed()`; on advance (confirmed by `is_soul_sealed`) they emit their own Learning→Operational on **their own** `tx` and fall through to the operational loop. Each stream still writes only to its own channel — no sender registry, no per-session keying (mode + `active_session` are brain-global). Fixes the desktop-stuck case AND the same-shaped first-run-**wizard** race (both clients in the wizard when one completes config). The previously-documented `/mode`/`/status` workaround **never actually worked** — the learning loop ignores slash commands, and even operationally `/mode` only prints text and never emits a `ModeTransition`; corrected in CLAUDE.md.

**Bug #2 (reconnection-briefing spam) — connect-time timeline snapshot.** The shipped `has_connected_once: RwSignal<bool>` filter (`2e8f20f`) flipped only on a *non-reconnection* frame, but the brain replays history as a *series* of `kind:"reconnection"` System frames followed by the flipping `Mode` frame; if iOS dropped the WS mid-replay the flag never flipped, so the next reconnect re-dumped the whole history. Replaced with a per-connection snapshot taken at WS-open in `run_ws_once` — `let had_content_at_connect = messages.with_untracked(|m| !m.is_empty());` — passed into `handle_server_msg`; `kind:"reconnection"` frames are suppressed iff the timeline already held content at connect. Robust to flaky reconnects, correct on cold load (empty timeline ⇒ the genuine first-connect briefing still shows). Client-side only (`chat.rs`), no brain change.

**Bug #1 (empty Tool bubbles) — a stale embedded frontend was masking shipped fixes.** The UI fixes (tool-card `(no output)` / `(no input)` / `(tool)` fallbacks, empty-`System`/`Assistant` guards) were correct and at HEAD but **were not reaching the device**: `embra-web` embeds `crates/embra-web-ui/dist` via `rust-embed` at compile time (`assets.rs`) and had **no `build.rs`**, so cargo had no `rerun-if-changed` edge from `dist/` and could skip recompiling embra-web when only the frontend changed — baking stale WASM into the image regardless of browser cache. New `crates/embra-web/build.rs` emits `cargo:rerun-if-changed` for the `dist/` root and every file under it. **This is now a load-bearing build requirement** — recorded in CLAUDE.md so the stale-embed trap doesn't recur. (The *separate* "tool card renders blank" symptom turned out to be CSS, not data — see the tweaks below.)

**Opus 4.8 as a selectable Anthropic model.** `AnthropicProvider` carries per-instance `model` + `display_name` (`new()` defaults to `claude-opus-4-7` / `opus-4.7`; new `with_model(key, model, display_name)`); the request shape (adaptive thinking + `effort: "max"`) is identical across Opus versions, so switching only swaps the `model` id — no other request-body change. New serde-additive `SystemConfig.anthropic_model: Option<String>` (no schema bump — follows the `max_tool_iterations` / `show_reasoning` precedent) + `resolve_anthropic_model` precedence (env `EMBRA_ANTHROPIC_MODEL` > config > default) with friendly aliases (`opus-4.8`, `4.8`, `claude-opus-4-8`, …). New `/model [opus-4.7|opus-4.8]` slash command — shows the active model, or rebuilds the provider for the next turn. The first-run wizard now offers both "Anthropic Claude Opus 4.7" and "Anthropic Claude Opus 4.8" entries; the chosen version persists to `anthropic_model`.

**Chat-mobile tweaks (operator-reported, post-E2E).** (1) Wizard validation/confirmation messages now show the **chosen** Opus version — the hardcoded "Opus 4.7" came from a `provider_label(provider_kind)` helper that ignored the model; three sites switched to the literal `provider_choice` and the helper was removed. (2) `/model` is now listed in the console `/help` (`embra-console/src/terminal/commands.rs` — the desktop console handles `/help` locally, not via the brain), the brain `/help`, the desktop sidebar + ⌘K palette, and the mobile slash picker. (3) Mobile JSON syntax highlighting in Tool cards, mirroring the TUI scheme (keys cyan, strings green, numbers/bools amber). (4) **Tool card render fix** — the card rendered completely blank on-device despite the content being present in the DOM; root-caused via DevTools element inspection (computed `height: 2px`): `.bubble.tool`'s `overflow: hidden` inside the `.chat-scroll` **flex column** flips `min-height: auto` → `0`, so `flex-shrink` collapsed the card to its 2px border height and clipped everything. One-line fix: `flex-shrink: 0` on `.bubble`. (~14 builds were spent theorizing about Leptos internals before inspecting the rendered DOM; the lesson — establish ground truth on the rendered output first — is recorded as a feedback memory.) Plan: `~/.claude/plans/there-are-three-bugs-immutable-snowglobe.md` (refines `bug-3-cross-stream-state-broadcaster.md`).

**`7e944c2`→`23bb058` — feedback-loop spec reduced to a steps-only checklist (v2.2, 2026-05-31).** A runtime token diet following the v2.1 tightening (above). `crates/embra-brain/src/brain/feedback_loop_spec_v2.md` went 275 → 92 lines: step headings + the literal command blocks are kept; all prose, examples, format scaffolds, the action-definitions glossary, and every reference table (query baseline, S0–S3 severity tiers, evaluation dimensions, decision framework, action→tool map) are dropped; Steps 2 & 3 collapse to bare headings; the empty "5.4 Token Usage Record" heading and the orphaned "Token usage" field in the 5.2 findings template are removed (`23bb058`). `build_feedback_loop_prompt()` framing prose is trimmed to match, **deliberately preserving** the governance-boundary line (S0/S1 auto-execute; S2/S3 pause at Step 4.2 for approval) and the Step 1.1/4.2 anchors — once the spec's own 3.4/4.2 are bare headings, the wrapper is the only in-prompt carrier of the approval rule. Wrapper spec label fixed v2.0 → v2.2 (the file header had drifted to v2.1; both now v2.2). The spec is `include_str!`-embedded and pasted verbatim into the synthetic user message on every `/feedback-loop` run, so each narrative line cost tokens at runtime; nothing parses it (string interpolation only), so the cut is purely operational. Brain rebuild required for runtime effect (`include_str!` is compile-time). 2 files across two commits, net +8 / −155. No proto/schema/tool-count change; embra-brain 455 tests pass.

**`f9c28b8` — web terminal switched to the xterm.js Canvas renderer (2026-06-03).** Operator-reported desktop-only bug: typing into the `:3345` web console's terminal, once a line wrapped, the characters visually bunched/overlapped. Root cause was xterm.js running its default **DOM renderer** (`crates/embra-web-ui/assets/` vendored only `xterm.js` + `addon-fit.js`; `embra-term.js` set no `lineHeight`/`letterSpacing`) — it lays each row out as `<span>` runs and lets the browser place glyphs by natural font advance, so on long/wrapped rows the glyphs drift off the integer cell grid (worse at non-100% browser zoom or fractional DPR). Confirmed web-only — the ratatui input widget already auto-grows to 10 rows on wrap (`embra-console/src/terminal/ui.rs`) and the `cols`/`rows` PTY flow (`embra-term.js` → `ws.rs` → `pty_bridge.rs` → `master.resize`) is faithful. Fix: vendor `@xterm/addon-canvas@0.7.0` as `crates/embra-web-ui/assets/addon-canvas.js` (lockstep with the vendored `@xterm/xterm@5.5.0` + `@xterm/addon-fit@0.10.0` — the minified assets carry no version in their filenames, so the versions were recovered by sha-fingerprinting against jsdelivr), add a `<script>` tag in `index.html`, and `term.loadAddon(new CanvasAddon.CanvasAddon())` in `embra-term.js` **after** `term.open()` (xterm requires renderer addons to attach post-open), try/catch-guarded so a load failure degrades to the DOM renderer rather than a blank terminal. The Canvas renderer paints every cell at `col × cellWidth`, eliminating the drift; Canvas chosen over WebGL as the low-risk default (no GPU context-loss path, ample for a TUI that full-repaints every ~200 ms). **Front-end only** — no Rust logic, no proto/schema/tool-count change; serial TUI (`EMBRA_TUI=1`) untouched; embra-web 15 tests green; trunk build clean; `dist/` is gitignored (regenerated by `build-image.sh`). 3 files (1 new vendored addon + `index.html` + `embra-term.js`), +12 insertions. William quick-tested in QEMU before commit. Plan: `~/.claude/plans/i-noticed-a-bug-scalable-mccarthy.md`.

**Guardian self-authoring + the soul-spec replicant check (2026-06-06).** The intelligence can now author its own Guardian dynamic tools — gated. The first concrete landing of SOUL-AS-ENFORCED-RUNTIME-01 Pillar 1 ("enforcement, not persuasion"), scoped to the self-authoring path. **Three gates:** (1) static `syn` validation; (2) the **replicant check** — when the brain calls the new `guardian_propose` tool, the backend constructs its own provider (via a `build_provider_from_config` helper extracted from the per-turn loop's inline construction) and makes an *independent, isolated* soul-verdict model call that judges the draft against the sealed soul (prose → model-judgment) and returns `{verdict: allow|refuse|escalate, touched_lines, rationale}` — the authoring brain never rules on its own draft, and the check **fails closed** (any error/unparseable verdict → no proposal); (3) operator approval — `allow`/`escalate` drafts persist as a new `ToolStatus::Proposed` doc carrying the verdict, which the operator approves with `/guardian approve <name>` (or `/guardian reject <name>`) to run the existing build pipeline; `refuse` is blocked with "did not pass the replicant check," naming the touched soul line. **The replicant check gates BOTH authoring paths** (William-directed): `/guardian-define` (operator paste) is soul-checked too — a `refuse` blocks the compile (the soul is not operator-waivable), while `escalate`/`allow` proceed (the operator already chose to paste, so they are the escalation target); skipped only before the soul is sealed. Shared via a `run_replicant_check` helper. New `crates/embra-brain/src/guardian/replicant.rs` (verdict call + tolerant JSON-object parser); `propose`/`approve`/`reject` + a verdict-aware `show` in `guardian/mod.rs`; `mark()` switched to load-merge so the verdict + original `created_at` survive `proposed→building→ready`; additive `ReplicantRecord` + optional `ToolDoc.replicant` field and the `Proposed` enum variant in `embra-guardian` (serde-additive — no `TOOL_DOC_FORMAT`/schema-v12 bump). **Tool count 92 → 93** (`guardian_propose`; only code change is the derived `CATEGORY_COUNTS` guardian entry 2→3). Strictly above apid — no proto/WardSONDB-schema/apid change; dynamic tools still never enter the provider snapshot. +7 embra-brain tests (455 → 462: verdict parser ×5, provider-dispatch, propose-args) / +1 embra-guardian (proposed-doc round-trip). Full propose→approve→build→invoke + the negative refuse path were **operator-verified on QEMU 2026-06-06** (live provider + sealed soul + in-OS toolchain). Same-session follow-ups: README callout updated + new `docs/REPLICANT-CHECK.md` (`513c69e`); the `GUARDIAN_TEMPLATE` copy-fill skeleton (embedded in the `guardian_propose` description and appended to validation errors, to cut the redraft loop) plus `/guardian approve`/`reject` surfaced in the web sidebar + ⌘K palette, the chat-mobile picker, and the console `/help` (`fc2bee2`). Plan: `~/.claude/plans/hi-opus-i-m-back-soft-eclipse.md`.

**Memory/KG search-window fixes + console input-layout fix (branch `knowledge-and-webui-fix`, 2026-07-02).** Two operator-reported production-scale bugs. **(1) Memory search "freeze" (FIX-1..8).** As William's production instance grew, `recall`/`memory_search`/`memory_scan`/`knowledge_query` appeared frozen at a late-May snapshot — an Embra-authored self-diagnosis blamed a "stalled FTS indexer" that does not exist. Real root cause (`EMBRA-SEARCH-FREEZE-DIAGNOSIS.md` v1.1, local): the memory/KG tools issued WardSONDB queries with **no explicit `limit` and no `sort`**, so the server default (`limit: 100`, primary-key order — UUIDv7 ⇒ oldest first) returned a fixed window over the *oldest* documents that never advances; the traversal edge layer had the same class live (unsorted hardcoded `limit: 200` vs measured hub degrees 259–302 — the newest 20–35 % of hub edges invisible). Fix is entirely embraOS query construction, per locked decisions D1–D3: new `WardsonDbClient::fetch_recent[_with_fields]` (explicit `MEMORY_FETCH_WINDOW = 10_000`, sort `_created_at desc, _id desc` — UUIDv7 tiebreak for a total order — plus a `wardsondb::window` saturation warning whenever a window comes back full) now backs `recall` + aliases, `memory_scan`, `memory_dedup` (a spec-inventory gap caught during implementation — same empty-body defect, including its cross-collection semantic pass), and retrieval's free-text step (which also had a latent second freeze at entry #500); retrieval's tag/session windows gained recency sorts and the session-edge window is ranked `weight desc, created_at desc` with the `memory.entries` exclusion moved server-side (`$ne` — safe: every edge doc carries `target_collection`) at limit 50; `fetch_turns` got an explicit `{limit:10, sort asc}` + a loud multi-doc warning; traversal's per-hop fetch is ranked and config-bounded via two new serde-additive fields `kg_traversal_edge_limit` (500, above the ~450 structural creation ceiling) and `kg_traversal_node_budget` (1000, BFS bound → `TraversalResult.truncated` + summary note), each with `kg::traversal` saturation warnings; `system_status` gained the FIX-6 parity check — authoritative `count_only` per memory collection vs the fetch window, surfaced as `wardsondb.memory_collections[]` + a top-level `search_window_saturated` flag (the correct version of the confabulated "monitor the indexer" idea). The O(n²) duplicate-candidate passes in `memory_scan`/`memory_dedup` are bounded to the newest 500 entries (noted in their reports) so a saturated window can't run into the 600 s global tool cap. Query-body shapes are locked by builder-level unit tests (no DB mock exists in the crate). User-visible shift: recall/search list newest-first; `knowledge_query` windows are recency-ranked. WardSONDB itself needed **no changes** (verified against its source @ `4a2f06f`: `count_only` exists, multi-field doc sort with preserved priority, `_created_at` stamped RFC3339-UTC on every doc). **(2) Console input overlap.** In the `:3345` desktop web terminal (200+ cols), once the input box soft-wrapped, the cursor rendered *inside* the typed text and typing became unreadable. Root cause: the input box rendered via ratatui `Paragraph::wrap` (**word**-boundary wrapping) while box height and cursor position used **character** arithmetic (`w / width`, `w % width`) — two different wrap algorithms disagreeing on every spaced wrapped line (Phase-0 vintage; latent on serial too). Fix: new pure `terminal/input_layout.rs` makes one greedy unicode-width char packing the single source of truth for rows + height + cursor (phantom-row convention preserved), renders pre-wrapped rows without `Paragraph::wrap`, and scrolls to keep the cursor row visible past the 10-row cap (previously the cursor escaped below the box). Deliberate serial deltas: mid-word wrap at the box edge (matches the conversation pane's existing char-wrap), cursor always at the insertion point, tall inputs scroll. **Docs:** TOOL-REFERENCE rows updated (windowed semantics; missing `search_memory` alias row added; `session_search`'s 20-cap documented as an intentional output bound), KNOWLEDGE-GRAPH.md retrieval/traversal/knobs sections updated (incl. the "No pruning logic exists" section rewritten as "No stored-data pruning exists" + the D1 two-layer read doctrine, and two stale 92-tool references fixed to 93). **Invariants intact:** 93 tools, no proto change, no schema bump (config fields serde-additive), system prompt untouched, `EMBRA_TUI=1` serial path shares only the deliberate input-box fix. embra-brain 463 → **479** tests (+16 shape/predicate guards); embra-console 21 → **31** (+10 input_layout). 3 commits (`344d036` brain, `69d1da7` console, + docs). Plan: `~/.claude/plans/for-this-work-will-witty-rose.md`. **Follow-up on the same branch (2026-07-03): `recall` gains `unpromoted_only: bool`** — a promotion worklist: only `memory.entries` with no `promoted_to` pointer, display cap 50 (vs 10) newest-first, `query` still narrows. The filter reads the **maintained** promotion pointer (`promoted_to` — set by `knowledge_promote`, cleared by `knowledge_unlink_node`'s cascade and the dangling-pointer repair), deliberately NOT a `derived_from` edge anti-join: edges drift (`unlink_edge`/`sweep_orphans`) with no repair path, the anti-join would cost an extra windowed edge fetch whose saturation would silently misclassify, and the pointer check is zero extra queries. Missing and null `promoted_to` both read as unpromoted (pre-field entries lack the key; the cascade PATCHes null). Aliases unchanged (always full search). Schema note: the recall tool schema changes → one-time prompt-cache invalidation; count stays 93. embra-brain 479 → **482** (+3: predicate ×3, plus arg/schema assertions). **Second follow-up, same branch (2026-07-03): windowless KG maintenance tools.** The production graph (~91k edges) was approaching the maintenance tools' 100k ceiling — `knowledge_graph_stats` pulled every edge doc through a fixed `limit:100000` window (and 10k node windows) and would go silently partial past it; `find_orphan_edges` had the same single-query cap. Embra proposed a `knowledge_clean_edges` bulk-delete of low-weight auto edges to stay under the window; rejected — deleted edges are unrecoverable (derivation runs only at write time for new docs; nothing re-derives between existing nodes), low-weight edges already lose at read time under FIX-7's ranked windows, and deleting stored data to fit a client window inverts the D1 doctrine. Instead the ceiling was removed: `knowledge_graph_stats` is now **windowless** — totals via server-side `count_only` (new `WardsonDbClient::count_filtered`; promoted = `{"promoted_to": {"$ne": null}}`, the filter form of the is-promoted predicate), category/edge-type distributions via aggregate `$group`, density from counts, exact at any graph size, and the ~91k-doc pull per stats call is gone. `find_orphan_edges` is **paginated** (20k-edge pages riding stable UUIDv7 key order — a deliberate, documented no-sort exception: exhaustive coverage, not a relevance window), so sweep coverage is bounded only by the `knowledge_sweep_orphans` `limit` (clamp raised 100k → 1M; default 10k unchanged; the 600s tool cap is the real backstop); the passive orphan check in graph_stats stays bounded at 100k but reports coverage against the exact edge total. No new tools (count stays 93; the sweep arg doc-comment change rides the same one-time cache invalidation as `unpromoted_only`); no schema/proto change. embra-brain 482 → **488** (+6: count-body builders ×2, group pipeline/parse ×2, promoted filter, orphan-page shape).

**`knowledge_dump` + `guardian_call data_file` + the committed kg_scan example (branch `knowledge-and-webui-fix`, 2026-07-03).** Landed the pipeline around the first intelligence-authored Guardian tool. **(1) `knowledge_dump` (tool 94).** JSONL export of the graph to `/embra/workspace/KG_DUMPS/kg-dump-<utc>.jsonl` — one meta header line, node lines (`type`/`_id`/`collection` lifted top-level, stored doc under `data`), edge lines (stored edge doc spread top-level + `"type":"edge"`); `collections` restricts to a subset (canonical order regardless of input order), `edge_types` filters the edge pass server-side via `$in`, `include_payload=false` emits slim node lines for scan-sized dumps. Each collection is tiled exhaustively via the sanctioned unsorted key-order pagination (`DUMP_SCAN_PAGE` 20k — the orphan-scan doctrine exception; verified against WardSONDB source that `offset`/`limit` apply post-filter in every executor path, so a constant filter tiles without skips or duplicates); per-collection written-vs-`count_only` parity is reported (soft signal); any query/write failure removes the partial file (the format has a header but no trailer — a partial would read as complete). Cron-able with zero extra work (`json!({})` → serde defaults → full dump). Learning `CATEGORY_COUNTS` knowledge_graph 9 → 10. **(2) `guardian_call` gains `data_file`** — the file → guest-input bridge. Guardian guests have zero filesystem access, so file-shaped data previously had to transit the model context as inline tool-call input. The optional path must resolve inside the workspace jail (shared `resolve_workspace_path`, now `pub(crate)`), is size-gated at 2 MiB (`GUARDIAN_DATA_FILE_MAX` = `FILE_READ_MAX`), and is read host-side and injected as the `input.data` string before dispatch; only valid with `action="invoke"`; a pre-existing `input.data` key is rejected (one source for `data`); omitted input upgrades Null → `{}` on this path only (the legacy no-`data_file` `"null"` serialization is untouched). No wasm/substrate change — no new capability, no scaffold/ABI edit; the guest still just receives its input string (the 8 MiB bump arena stays the guest-side bound; ~1 MiB guidance for parse-heavy tools). **(3) kg_scan committed as [GUARDIAN-KG-SCAN-EXAMPLE.md](GUARDIAN-KG-SCAN-EXAMPLE.md)** — the first intelligence-proposed, soul-gated, operator-approved Guardian tool (drafted on the production instance via `guardian_propose`, iterated v1 → v5 on 2026-06-03), committed **byte-faithful to the v5 artifact** and registered in the `doc_examples_validate` drift-guard (now ≥6 modules across three docs). The surrounding prose corrects three of the authoring intelligence's own misconceptions instead of editing its code: the stale `http_get`+`web_search` caps claim (the module declares none — pure compute), the `data`-as-path workflow that never worked (the guest iterates `data.lines()`; `data_file` is the real bridge), and the ULID mislabel (embraOS `_id`s are UUIDv7 hex, on which the module's Crockford decode is order-preserving with ~256 ms tie granularity — `temporal_gating` still correct). One-time prompt-cache invalidation (+1 tool + the guardian_call schema change, bundled); no proto/DB-schema change. Docs: TOOL-REFERENCE (94 + `knowledge_dump` row + `guardian_call` `data_file` sentence + worked-examples line), KNOWLEDGE-GRAPH (ten tools, `knowledge_dump` maintenance entry, refreshed stale line refs, a leftover 92 → 94), SYSTEM-DESIGN counts ×3, docs/README Guardian row. embra-brain 488 → **506** (+10 dump shape/args guards incl. the no-sort page-body test, +8 data_file gate/jail/inject/load). Root README updated at William's direction in a same-day follow-up commit: the eight 93-count sites → 94, the memory/KG callout's "nine `knowledge_*` tools" → ten, a **Guardian KG-Scan Example** chapter-table row, and the replicant-check callout now names `kg_scan` as the first tool through the gated path. 3 commits (`33dead0` dump, `96e54af` bridge, `cd4e241` docs) + the README follow-up. **Same-branch fix from William's operator testing (2026-07-03): `knowledge_traverse` was blind to structural edges.** The per-hop filter matched `source_id`/`source_collection` only (outgoing edges — Sprint-2 vintage, unchanged by FIX-7, verified against the pre-branch tree). Auto-derived edges are double-written (`push_bidirectional` — both directions exist as docs) so they always looked outgoing; brain-created edges (`knowledge_link`) and `derived_from` are stored as ONE directed doc — invisible from every node except their source, i.e. from precisely the foundational nodes that `enables`/`depends_on`/`refines` point *at*. Symptom: traversal appeared to follow only `same_session`/`temporal`/`tag_overlap`. Fix: **undirected expansion** — each hop queries edges touching the node from either side (`hop_edge_filter`: `$or` over the source and target arms, the same shape as the `knowledge_unlink_node` cascade; WardSONDB's parser ANDs sibling `edge_type`/`weight` keys with `$or`, verified in its `filter.rs`), and the neighbor is the *other* endpoint (`neighbor_of`). Result edges keep their true stored direction (no reverse docs are written — a reverse `enables` would assert a false statement; read-side reachability also retroactively fixes the entire existing graph with zero data changes, `related_to` included). The visited check dedupes bidirectional twin docs, so `result_edges` stays a spanning set. Locked follow-on decisions: the single 500-edge ranked window now covers both directions (an auto link's twin docs take two slots — saturation warns fire somewhat earlier; per D3 the escalation stays a type-partitioned fetch, not a cap raise), and NO `direction` arg was added (no schema change — not even a prompt-cache event). Side benefit: `knowledge_query`'s Step-4 depth-2 graph expansion calls `traverse` directly, so retrieval now surfaces structural context it had never seen (`graph_expansion` ranking shifts are expected). embra-brain 506 → **511** (+5: `$or`-arms/sibling-key/omission shape guards ×3, `neighbor_of` outgoing/incoming ×2; the old source-only `edge_body_preserves_filter_keys` reworked to the new shape).

**KG retrieval performance rework — arm-split indexed traversal + NodeStore prefetch (branch `knowledge-and-webui-fix`, 2026-07-04).** Production `knowledge_query` calls were taking **5–8 minutes**, and auto-enrichment runs the same retrieval on every user turn ≥15 chars — per-turn latency at ~99k edges. An Embra self-diagnosis blamed the sequential promotion-redirect point reads; re-benchmarking against a local copy of the production WardSONDB showed that path costs ~1–2 s (~100× too small) and pinned the real cause: **WardSONDB's planner sends every `$or` filter to a full collection scan** (verified in its `planner.rs` — Or/Not → FullScan; parse-all, sort-all, slice), and the 2026-07-03 undirected-traversal hop used exactly that shape — ~300–420 ms per hop, one hop per expanded node, from 10 HashMap-order seeds at depth 2 in a ~75%-dense graph ≈ 1,300–2,600 full scans per query. The same lookup split into its two equality arms rides `idx_edge_source`/`idx_edge_source_target`/`idx_edge_target` at ~0.6 ms with a **provably identical** result window (any member of the union's top-K is in its own arm's top-K; empirically set-identical on real hub nodes; only exact weight/`created_at` ties at the truncation boundary can differ, now made deterministic by an `_id desc` tie-break the server never had). Landed, all embraOS-side (WardSONDB unchanged): **(1) arm-split hops** — `hop_edge_filter` (`$or`) replaced by `source_arm_filter`/`target_arm_filter` (id+collection as top-level sibling equality keys — the planner contract; `edge_type $in`/`weight $gte` ride both arms) + client-side `merge_arm_edges` (server-comparator mirror, `_id` dedupe, saturation = either arm full or merged overflow), guarded by `hot_path_arm_bodies_never_contain_or`; **(2) multi-source level-BFS** — `traverse()` → `traverse_multi(starts, …, store)`: retrieval Step 4 became ONE call over the top-10 *scored* seeds (`seed_keys`, shared `score_one`/`build_score_ctx` core + deterministic tie-breaks; was 10 independent HashMap-order walks re-scanning ~90%-overlapping neighborhoods), `knowledge_traverse` passes one start (output semantics unchanged; budget now global per call); per-level arm queries run at `HOP_CONCURRENCY` = 8 (module const, not config — no new `SystemConfig` fields); **(3) `NodeStore` prefetch-join** (new `knowledge/node_store.rs`) — semantic + procedural fetched once per call (`fetch_recent`, saturation-warned), Step 1 tag matching moved **in-memory** with the exact server `$contains` semantics (case-sensitive membership via `doc_tag_contains`; newest-20-per-tag via the `sort_created_desc` comparator mirror — a long enrichment message no longer costs 100+ per-tag round trips), Step 2 targets / Step 3 promotion redirects / traversal node loads all resolve from the store with a cached point-read fallback; **(4) access-touch diet** — per-BFS-visit `spawn_access_touch` (read+PATCH per visited node; thousands of background writes per query, pushing `access_count` toward "times swept" noise) replaced by one `spawn_access_touches` task touching only **returned** nodes (retrieval top-K; the tool's returned set) — William-approved semantics change, `access_frequency` now measures retrieval hits; **(5) slow-query observability** — the brain's client now reads the (previously discarded) response `meta` and warns under `wardsondb::slowquery` when a query runs ≥100 ms or scans ≥10× what it returned above a 1,000-doc floor. Cold `$or` cascade deletes (`forget`, `knowledge_unlink_edge`/`unlink_node`) stay — commented as never-copy-into-hot-paths. **Measured** (local copy of the production DB, 99,417 edges): the worst benchmark query (`epoch`-class, ~300 candidates → 10 hub seeds) went **~417 s → 1.86 s** (~224×, sequential — before the level concurrency) with node coverage *increased* (779 visited vs 720 per old single seed); hop query 418 ms → 1.3 ms (~330×); `zebra`-class no-seed queries unchanged (~31 ms). Docs: KNOWLEDGE-GRAPH (collection steps, ranking, traversal + "why arm-split", access-count section, the stale "cached-after-first" enrichment claim), TOOL-REFERENCE (`knowledge_traverse`/`knowledge_query` rows). No proto/schema/tool-count change (94; tool descriptions byte-identical — no prompt-cache event). embra-brain 511 → **528** (+17: arm/merge/expand/seed-dedup shape + semantics guards, NodeStore + `$contains`-mirror + comparator tests, step1/seed/scoring-formula locks, slow-query threshold triple; the four old `hop_filter_*`/`$or` guards retired with the shape they pinned). 3 commits (`6c16f1e` observability, `b24414e` core rework, `0f2edd6` docs) + William's QEMU-defaults chore `742392c` (both run-qemu scripts 2G/2cpu → 4G/4cpu) riding the same wave; operator-tested on PRODUCTION ("performance is excellent") and FF-merged to `main` @ `742392c` + pushed 2026-07-04. Follow-up docs pass same day: the stale "why two records per relationship" FAQ rationale in KNOWLEDGE-GRAPH.md rewritten to the post-arm-split truth (the double-write is storage convention now, not reachability machinery), plus the Phase-0 `assets/embraos-banner.html` retired (superseded by the SVG→PNG pipeline of `2e471be`). Plan: `~/.claude/plans/now-that-we-have-parallel-tarjan.md`.

**embra-web console promoted to stable (2026-07-05).** Docs-only. The web console's **experimental** designation is removed across the operator-facing docs — William's call after ~7 weeks as the default UI: merged 2026-05-16 (`acb241e`), desktop xterm.js + chat-mobile modes in daily operator use since, last known UI bugs closed by the embra-brain-broadcaster wave 2026-05-29 (`7f6c479`). Touched: README's Quick-Start callout (⚠️ dropped), `docs/QUICK-START.md` (callout + build-block comments), `docs/AARCH64-BUILD.md` + `docs/INTEL-MAC-BUILD.md` (default-UI callouts; their joint "embra-web and embra-guardian-v1 remain experimental" status lines now name embra-guardian-v1 alone), and `docs/ROADMAP.md` (embra-web row 🔬 → ✅ — default UI, stable, promoted 2026-07-05). Still experimental: `embra-guardian` v1, `/feedback-loop`, and the SSH tool family. Historical entries in this log keep their contemporaneous "experimental" wording by design. The serial TUI (`EMBRA_TUI=1`) remains the no-rebuild fallback. Same-day cover hygiene `8fb472b`: README's Current Status dropped its stale "(embra-desktop branch is experimental)" parenthetical — that branch was deleted 2026-07-03. No code change; no proto/schema/tool-count/test movement.

**Fable 5 + Anthropic provider optimizations (branch `llm-provider-optimizations`, 2026-07-06).** Four coordinated Anthropic-provider upgrades, William-directed. **(1) `claude-fable-5` selectable + the default flips to Opus 4.8.** `/model` gains `fable-5` (aliases `fable-5|fable5|fable|claude-fable-5`); the first-boot wizard gains an "Anthropic Claude Fable 5" entry; the web ⌘K palette selector, console `/help`, and brain `/help` list all three models. The existing request body needed **no change** for Fable 5 — adaptive thinking + tunable effort + 128k `max_tokens` with no sampling params is valid on it unchanged (Fable 5 400s on `thinking:disabled`, `budget_tokens`, and `temperature`/`top_p`/`top_k`, none of which embraOS sends; body construction is now an extracted pure `request_body()` with a per-model shape regression test). `DEFAULT_MODEL` moves `claude-opus-4-7` → `claude-opus-4-8`: every wizard label now seeds `anthropic_model` **explicitly** (a 4.7 pick keeps meaning 4.7 across future default moves; previously the 4.7 label seeded `None` = "ride the default"), the wizard's default selection is the 4.8 label, and a config with `anthropic_model` unset resolves to 4.8 from its next boot — a one-time model-scoped prompt-cache switch for deployments riding the default. Ops note: Fable 5 requires 30-day API data retention — a zero-data-retention org gets `400 invalid_request_error` on every request (surfaces through the existing `Brain error: API error 400` path; check org retention before debugging request bodies). **(2) Refusal-as-error — no fallback, ever.** `stop_reason: "refusal"` previously shared the silent `EndTurn` break and persisted `"(no response)"` — a refusal was indistinguishable from a quiet turn. The SSE parser now captures the refusal's `stop_details` (`category`/`explanation`, all reads Option-guarded — additive on the wire and the neutral IR: `AssistantTurn.stop_details`), and a post-loop `terminal_outcome_notice()` emits an operator-facing **Error** frame naming the category/explanation, a **Warning** frame on `max_tokens` truncation, and a generic Error for the other early stops (Gemini `Safety`/`Recitation`/`Malformed` and `StopSequence` get honest frames for free). Session history stays honest via `final_assistant_text()`: an empty refusal persists `"(request declined by provider: refusal)"` (instead of `"(no response)"`), a refused partial gains a `"(response interrupted by provider refusal)"` suffix. Deliberate design point: the Anthropic API's server-side refusal fallback (`fallbacks` param + beta header) is **opt-in and embraOS does not opt in** — the OS gives a single model a sealed identity, and silently re-serving a declined turn on a different model would be a continuity violation; refusals surface, period. **(3) Streaming-client hardening.** The hot-path reqwest client was `Client::new()` — no timeouts; a dead connection hung the turn forever. Now `connect_timeout` 10 s + per-read idle `read_timeout` 300 s (resets on every byte; the API's periodic SSE pings keep any live stream far under it). Deliberately **no total request timeout** — Fable 5 turns legitimately run many minutes at high effort. **(4) `/effort`.** `output_config.effort` was hard-coded `max`; it's now runtime-tunable (`low|medium|high|xhigh|max`, default `max`) mirroring `/model`: serde-additive `SystemConfig.anthropic_effort`, `EMBRA_ANTHROPIC_EFFORT` env override, strict allowlist (no unknown-value passthrough — an invalid effort would 400 every turn, so bogus values fall through to the default), `AnthropicProvider::with_effort` threading, takes effect next message; surfaced in both `/help`s, the web sidebar/palette, and the chat-mobile picker. **(5) `set_name` — the intelligence can change its own name (tool 95).** During Learning Mode the intelligence is literally asked "Is the name {name} right?" but a renegotiated answer only ever landed in the identity document — `SystemConfig.name` (the `You are {name}` prompt line, the console `[timestamp] Name:` prefix, the status bar) kept the wizard value forever, and post-seal there was no mechanism at all. Two halves: **pre-seal**, `handle_phase_complete`'s Phase-2 arm now syncs a valid identity `name` differing from the config name into the persisted config (`sync_name_from_identity`; the learning loop adopts it for phases 3–6 prompts, tells the operator, and re-emits a Learning ModeChange so the console follows mid-learning — learning dispatches no tools, so a write-time sync is the only pre-seal path); **post-seal**, the new `set_name` tool (Self-Awareness 4 → 5) validates via the shared `config::validate_intelligence_name` (single line, 1–40 chars, no control chars, no ` — ` separator), persists config + the identity doc's `name`, and the operational tool loop re-emits an Operational ModeChange after a successful call so the prefix/status bar refresh mid-turn (tools carry no `tx` — the loop-side companion mirrors `/model`'s recipe). The tool description hard-requires explicit operator agreement in-conversation. **Soul-safe by construction:** the sealed soul schema (`purpose`/`ethical_lines`/`values`/`surviving_constraints`) never contains the name, and both embra-brain and embra-trustd hash only the inner `soul` value — a rename can never trip the boot-time hash HALT. New guard test `category_counts_sum_matches_registry` pins the hand-maintained Phase-4 taxonomy to `REGISTRY.len()` (previously the sum was only a comment). **(6) Expression-panel scroll (embra-console).** The EXPR-01 panel showed a fixed 6-row tail of live reasoning (and head-clipped long `express` content with the overflow unreachable) — unusable for reviewing a long thinking trace. New `expression_scroll` offset (rows-from-bottom, mirroring the conversation pane's semantics) on `Shift+Up/Down` (`Shift+PageUp/PageDown` = 5 rows; arms placed before the bare-arrow conversation-scroll arms — the `_` modifier wildcard would otherwise swallow Shift, the exact shadowing that makes the pre-existing dead `Alt+Enter` arm unreachable); both panel sources now render through the same offset-aware tail window (`window_to_visible_rows` gains the `skip_from_bottom` param — long `express` content shows its tail by default now, all of it reachable by scroll); offset snaps to 0 at every `live_reasoning` clear site (via the new `AppState::clear_live_reasoning` seam), on new polled expression content, and is deliberately preserved across `ResponseDone` (the operator may be mid-review when the turn ends); no-op while the panel is hidden (shared `expression_panel_visible` gate). Keyboard-only — no mouse capture, so the serial path shares the feature identically (the input-box char-wrap precedent); `docs/OPERATION.md` shortcut table updated. **Invariants:** no proto change, no schema bump (both new config fields serde-additive); tool count 94 → **95** (`set_name`) — a one-time prompt-cache invalidation on upgrade, and a name change is itself a one-time cache reset (the name is in the byte-stable operational prompt, `/model`-style). Serial TUI shares only the deliberate scroll/name features (no PTY-path divergence). embra-brain 528 → **549** tests (+17 provider/effort/refusal wave: model/effort resolver + alias guards, wizard-label seeding, request-body shape ×2, refusal `stop_details` SSE/conv parsing ×3, terminal-notice + persistence-honesty suite ×7, effort serde; +4 rename wave: name-validator ×3 + the CATEGORY_COUNTS↔registry guard); embra-console 31 → **39** (+5 offset-window, +3 scroll-reset lifecycle); embra-guardian 53 / embra-web 15 unchanged. Root README's seven 94-count sites folded to 95 at William's direction (same pass; `.zenodo.json`/`CITATION.cff` keep "94-tool" deliberately — they describe the released v0.7.0 artifact and get corrected at the next release bump, the v0.7.0 precedent). 7 commits (`0d3792b` brain provider wave, `d753263` console+web-ui surfaces, `005f475` docs, `1418d42` brain rename wave, `45abf11` console scroll, `942ea47` docs, + the README count pass). Plan: `~/.claude/plans/on-a-new-branch-zippy-grove.md`.

**Anthropic transient-error retry with exponential backoff (branch `llm-provider-optimizations`, 2026-07-08).** William hit `529 Overloaded` testing Fable 5 — and the Anthropic provider was the only one of the three HTTP backends with no retry (Gemini has a full backoff ladder, OpenAI-compat a single 5xx retry): any transient failure surfaced immediately as `Brain error: API error 529: …` and killed the turn. The initial POST now goes through a new `send_with_retry` helper mirroring Gemini's (same `[1, 2, 4, 8, 16, 32, 60]`-second ladder; per-attempt `warn!` under `anthropic::http`), with three deliberate deviations: the retryable set is SDK-parity **408/409/429/5xx** (what Anthropic's official clients retry — a superset of Gemini's 429/5xx) plus network send errors; the terminal error is a single `SendError` value carrying `{status, body, attempts}` (fixing the template's split `last_status`/`last_body` tracking, which could pair a stale status with a later network-error message); and the backoff ladder is a parameter, so the wiremock tests run at zero delay. `Retry-After` is honored when present (integer-seconds form, capped 60 s, read before the body is consumed). The retry boundary is **the initial POST only** — send + status check before any SSE byte; an established stream is never retried mid-flight (a partial response cannot be recovered), and the loop lives inside the provider's existing `tokio::spawn`, so `stream_turn` still returns the stream immediately and terminal failures keep arriving in-stream on the error path every caller is built around (continuation-turn persistence semantics unchanged). All seven `stream_turn` call sites benefit with zero changes: the operational tool loop, the learning loop (a transient 529 mid-Learning no longer kills the phase), and the guardian replicant check (a transient 529 no longer insta-rejects a tool proposal; a genuine outage still fails closed after the ladder, ~2 min worst case). Exhausted retries surface as `API error {status} after {n} attempts: {body}` — single-attempt failures keep the historical byte format. Operator visibility during backoff is the thinking spinner plus the `anthropic::http` warn lines (attempt/status/retry_after_secs); no new stream event. No proto/schema/tool-count change (95); a retried request is byte-identical to the first attempt — no prompt-cache event. embra-brain 549 → **556** tests (+7: retryable-set / Retry-After-parse / wire-format pure fns, wiremock recovery / exhaustion / non-retryable-400 / network-exhaustion). Landed as `4aa05fd` (feature) + the docs pass. Plan: `~/.claude/plans/on-a-new-branch-zippy-grove.md` (overwritten by this follow-up wave).

**Cross-provider session-block message spells out the full navigation (branch `llm-provider-optimizations`, 2026-07-08).** Operator-reported: attaching or `/switch`ing to a session pinned to a different provider printed "Use `/provider {kind}` to continue, or `/new <name>` to start a fresh session" — but `/provider` refuses to run while a session is active, so following the advice bounced the operator into a second refusal ("Cannot switch providers while a session is active… Run /close to close it first"). The three block sites (`SessionAttach`, `/switch`, and the `switch_session` gRPC RPC — previously three hand-rolled variants) now share one state-aware `cross_provider_block_message()`: with a session attached it names the full sequence — `/close` (detaches the named current session), then `/provider {kind}`, then `/switch {name}` — and with no active session it skips the `/close` step (there `/provider` works directly); both variants keep the `/new <name>` stay-on-current-provider alternative and now name BOTH providers instead of only the session's. The `/provider` command's own active-session refusal is unchanged (it was already accurate — it is the message the old advice crashed into). embra-brain 556 → **558** tests (+2: step-ordering contract `/close` < `/provider` < `/switch` with a session attached; no-`/close` variant without). No proto/schema/tool-count change.

**`3d33b12` + same-day follow-through — recommended-models revision: vetted local-inference list 4 → 2 + the MoE active-parameter rationale (2026-07-10).** William's continued live testing narrowed the vetted full-toolset list from four models to two: **`DeepSeek-v4-Pro:cloud`** and **`Qwen3.6-27b`** (Ollama tag `qwen3.6:27b`; LM Studio tag `qwen/qwen3.6-27b`, 8-bit) — the retired Qwen3.6-35B-A3B MLX pair and Qwen3.5-9b fall below the newly-articulated threshold. The README callout now carries the rationale: MoE models need a minimum active-parameter count (~27–49B active) for honest instruction-following under complex multi-step protocols — total parameters measure stored knowledge, active parameters working memory; below the threshold a model has enough knowledge to sound authoritative but not enough active capacity to track what it has actually done. Dense models have no total/active split (27B means 27B active); DeepSeek-V4-Pro clears it as 1.6T-total MoE with 49B active per token. `docs/RECOMMENDED-LOCAL-MODELS.md` was trimmed (the per-host Active Testing Roster tables, the launchd env-var block, and the Verification section removed) and re-anchored in the follow-through pass with a **Vetted Models** section — threshold note + the two-model local roster + the existing Ollama Cloud table — so the doc carries the same list the README links to; the follow-through also restored the three `OLLAMA_*` env-var names in compact form, corrected "mod file" → Modelfile (`PARAMETER num_ctx`), and refreshed the stale "per-family" pointer descriptions (README chapter table, docs/README.md, and SYSTEM-DESIGN.md's reasoning-controls pointer) to "vetted models and server configuration". A second same-day follow-up retired the doc's `88a2cfb`-era "Max-thinking response-signature confirmation in QEMU still pending" caveat — William confirmed the verification in production use (the historical `88a2cfb` entry below keeps its contemporaneous wording by design). Docs-only — no code, proto, schema, or tool-count change (95); no prompt-cache event.

**Session-UX fix trio — real turn counts, resume-briefing idle gate, no-session prompt guard (branch `session-ux-fixes`, 2026-07-11).** Three session-layer defects from William's mobile-web testing, all in embra-brain. **(1) Real session turn counts.** The chat-mobile sessions sheet showed "0 turns" for every session because the brain's `list_sessions` RPC hardcoded `turn_count: 0` — `SessionMeta` never tracked a count, and only `switch_session` derived one from history length (the proto field, the web `/api/sessions` mapping, and the sheet's rendering were all faithful transports of the manufactured zero). `SessionMeta` gains a serde-additive `turn_count` (no schema bump — the `anthropic_model` precedent; one construction literal): `create` stamps 0, `append_message` re-stamps `turns.len()` into the meta rewrite it already performs every turn (free), and the new `SessionManager::list_with_counts()` derives unstamped pre-fix metas from their history doc and backfills the stored meta fire-and-forget via `patch_document` (the `spawn_access_touches` precedent; a failed derivation displays 0 and is never persisted — no guesses on disk). `ListSessions`/`CreateSession` now map the real value; the TUI `/sessions` line gains `N turns` for parity; **no frontend change** — chat.rs already deserialized and rendered the field. Housekeeping in the same pass: the FIX-5 `history_query_body()` (explicit limit + oldest-first, canonical-first-doc guarantee) relocates from `tools/sessions.rs` to `sessions/mod.rs` as its canonical home and replaces the module's remaining empty query bodies (`load_history`, `append_message` ×2, `update_state`, `list`) — zero behavior change on one-doc-per-session collections, but the module now rides the explicit-window invariant. **(2) Resume-briefing idle gate + attempt cooldown.** On mobile, every WS background/lock-flap reopened `/ws/chat`, and the client re-attaches on every socket open (load-bearing: it re-resolves the active session after brain restarts and repopulates the timeline after iOS tab reloads) — but each re-attach is a fresh Converse stream, and the `SessionAttach` briefing gate (`!new_session && is_sealed && history_len > 0`, unchanged since 2026-05-21) had **no debounce of any kind**: one full LLM recap turn per flap. The `7f6c479` client-side filter everyone credited with fixing this only ever suppressed the `kind:"reconnection"` history-replay frames — the briefing's ordinary `Token`/`Done` frames were never filtered, and the recap got more conspicuous (and slower, hence more likely to be dropped mid-turn and re-fired) as the default model got heavier (Opus 4.8 + effort max). The gate now additionally requires the session **idle ≥ `RESUME_BRIEFING_MIN_IDLE_SECS` (30 min)** vs `meta.last_active` — safe by construction: nothing bumps `last_active` on a stream drop, and a *completed* briefing persists its turns and thereby bumps it, so the gate is self-limiting — plus a **120 s per-session attempt cooldown** (`SessionManager.briefing_attempts`, runtime-only `Instant` map) covering the aborted-mid-recap flap loop the idle gate can't see (an aborted turn never persists, so `last_active` stays stale). A missing/unreadable meta fires (pre-gate behavior on broken-meta paths); negative idle (clock skew) suppresses. Suppressions are observable: `info!` under `target: "sessions"` with `idle_secs` / `min_idle_secs` / `suppressed_by = idle-gate|attempt-cooldown`. `/switch` still briefs **unconditionally** (an explicit user action, not a transport reconnect) but records the attempt, so a WS re-attach seconds after a `/switch` briefing is cooldown-suppressed. Both consts are module-level in grpc_service.rs, deliberately not `SystemConfig` fields. **(3) No-session prompt guard.** With `active_session = None` (routinely reached via `/close` — including the `/close` → `/provider <kind>` flow the cross-provider block message itself prescribes), a plain prompt fell back to a literal `"default"` session name: the model answered with empty history, both `append_message("default", …)` calls silently no-opped against the nonexistent collection — a **ghost turn persisted nowhere** — and the path never ran `check_session_provider`, so a real session named `default` pinned to another provider would have been silently contaminated and appended to under the wrong provider. The UserMessage arm now blocks instead: a state-aware `no_session_block_message()` (the wave-4 guard-family style) names the most recent resumable session's `/switch` when one exists, else just `/new`, always pointing at `/sessions`; delivered as the same System+Error frame shape as the cross-provider block (error bubble on chat-mobile, plain render on the TUI), and the model is never called. Placement is load-bearing: after the sessionless setup intercepts (so `/provider --setup` key/reconfigure flows keep consuming messages post-`/close`) and slash commands are a different match arm — recovery (`/new`, `/switch`, `/sessions`, `/provider`) is never blocked. Deliberate side effects: `/feedback-loop` with no session attached now gets the block instead of ghost-running (its turns were persisting nowhere), and a failed learning loop's sessionless prompts block instead of ghost-running with the placeholder prompt. **Same-session follow-ups — `/sessions` last-active in the configured timezone (`191bca3`) + listing sorted most-recently-active first (`cc6ccb1`).** William's last catches: the listing rendered `meta.last_active` as bare UTC, and listed sessions in collection-name (alphabetical) order. New `format_local_timestamp` converts through the operator's configured timezone (fresh config wins over the threaded boot value — the SessionAttach ModeChange pattern, so a runtime timezone change shows immediately), resolving abbreviations via `tools::resolve_timezone` (BUG-007) and rendering the DST-aware zone abbreviation (`%Z`, e.g. `2026-07-11 09:30 EDT`); an unparseable timezone falls back to explicitly-labeled UTC, never an ambiguous bare stamp. And `list_with_counts` now sorts through `sort_by_last_active_desc` — most-recent first on both operator surfaces (the `/sessions` output and the chat-mobile sheet render the vec as-is); stable sort keeps alphabetical order as the tie-break for identical timestamps; `list()` (feeding the order-independent `get_most_recent_active`) is untouched. **Invariants:** no proto change (the `turn_count = 3` field existed since the proto's founding), no schema bump, tool count 95 unchanged, no operational-prompt or tool-schema change — **no prompt-cache event**. embra-brain 558 → **576** tests (+18: turn_count serde shape ×3, attempt-cooldown map ×4, idle-gate thresholds ×3, block-message structure ×2, local-timestamp render ×4, listing order ×2; `history_query_tests` relocated with its builder, net-zero). embra-console 39 / embra-guardian 53 / embra-web 15 unchanged. Landed as `b5db3cf` (feature) + `fd04586` (docs) + `191bca3` (timezone) + `cc6ccb1` (listing order) + their docs fold-ins. Plan: `~/.claude/plans/i-am-here-for-dapper-mango.md`.

**Vetted local-inference roster narrows 2 → 1 — `DeepSeek-v4-Pro:cloud` removed as cloud-hosted, not local (2026-07-14).** William narrowed the vetted full-toolset roster to **`Qwen3.6-27b`** alone (Ollama `qwen3.6:27b`; LM Studio `qwen/qwen3.6-27b`, 8-bit): the `:cloud` tag routes through Ollama's hosted infrastructure, so it doesn't belong in local-inference guidance — a scope correction, not a quality verdict. The README callout keeps the MoE active-parameter rationale verbatim as general guidance (MoE models require a minimum active-parameter threshold for honest instruction-following: total parameters measure stored knowledge, active parameters working memory; below ~27–49B active they become confabulation-prone under complex multi-step protocols — enough knowledge to sound authoritative, not enough active capacity to track what they've actually done) plus the dense-model corollary (27B means 27B active); removed from the callout: the DeepSeek threshold-example sentence, the model itself from the vetted list, and the auto-Max-thinking note. `docs/RECOMMENDED-LOCAL-MODELS.md` drops the whole Ollama Cloud section (table + notes), the orphaned Ollama-Cloud fleet-mapping bullet, and the `:cloud` env-var-bypass clause — the `num_ctx`-not-settable fact (`ollama#7063`) and its Modelfile workaround stay, scoped to locally-loaded models. The auto-Max-thinking route stays live in code and stays documented: `reasoning_effort_for_model` (`crates/embra-brain/src/provider/openai_compat/mod.rs`) still sends `reasoning_effort: "max"` for any model name containing `deepseek-v4-pro`, and the note relocates to SYSTEM-DESIGN.md's reasoning-controls-per-family list beside the DeepSeek-R1/R2 line — it fires whenever the model is operator-selected at wizard time, vetted or not. Docs-only — no code, proto, schema, or tool-count change (95); no prompt-cache event.

**WardSONDB code-review compatibility wave — boot-ensured `idx_edge_source_id`, cursor-adaptive maintenance scans, `embra.dbverbose=1` (branch `wardsondb-optimizations`, 2026-07-16).** A source-level compatibility audit of WardSONDB's `code-review` branch (73 commits ahead of its main; William's parallel-regression embraOS rebuilds already build against it by design) against every wire assumption embraOS makes found **one real incompatibility**: the branch's F2 planner fix stops serving single-field lookups from compound indexes (correct in general — compounds exclude docs missing any component field, and which compound answered was HashMap-arbitrary), which silently de-indexed the KG traversal **source arm** (`{source_id, source_collection}` equality — the only source-side indexes were the `source_id`-leading compounds) into a full `memory.edges` scan per hop: a partial regression of the 2026-07-04 KG performance rework at production edge counts (~99k), invisible on a small test graph. Fix: `ensure_hot_path_indexes` asserts a real single-field `idx_edge_source_id` on **every boot**, deliberately outside the versioned migration ladder — pre-F2 servers misdetect that create as a compound-duplicate (409, which `create_index` treats as idempotent success), so a one-shot migration would have recorded itself applied with the index never created and never re-run post-upgrade; the per-boot ensure self-heals across server builds, and on pre-F2 builds the missing index is harmless (their planner still rides the compound prefix). The target arm was never affected (`idx_edge_target` is single-field since v5); every other embraOS query shape was verified still indexed under F2 (session-edge / dup-check / `derived_from` = two-field `compound_eq`; `{session}` = single-field). Second, `knowledge_dump` and the orphan sweep adopt the branch's **cursor pagination adaptively**: page 1 keeps the offset body, and when the server offers `meta.next_cursor` (emitted on no-sort full scans exactly while matches remain) later pages resume by token — O(n) total server work instead of offset tiling's O(n²) re-skips, ending exactly when the cursor is withheld; pre-cursor servers keep byte-identical offset tiling (new `query_with_meta` client method exposes the envelope `meta` beside `query()`). Third, WardSONDB's per-request log lines became opt-in on the branch (the sync-log fix the performance handoff asked for — the default tmpfs log is now lifecycle/warn/error only), so `EMBRA_DB_VERBOSE=1` on `run-qemu.sh` (both arches) sets `embra.dbverbose=1` on the kernel cmdline and embrad appends `--verbose` to the wardsondb args (only valid on builds that know the flag; older builds reject it and won't start). Audit non-issues, verified at source on both sides: post-filter `offset`/`limit` and unsorted key-order survive the branch's streaming-executor rework (the dump/orphan tiling invariant holds), the uniform-404-on-missing-collection change can't reach embraOS's fragile callers (all queried collections are migration-created; the full-scan-shaped session/config queries already 404'd on the old build), the new sort `_id` tie-break converges with `merge_arm_edges`' client-side tie-break, and the operator/count/response shapes embraOS pins are byte-compatible. embra-brain 576 → **582** tests (+6: the ensure-list single-field pin, cursor/offset body shapes, and the `next_scan_page` mode transitions incl. never-fall-back-to-offset-mid-cursor-walk and non-string-cursor rejection). No proto/schema (v12)/tool-count (95) change; tool descriptions untouched — no prompt-cache event. Landed as `fa80865` (brain) + `b0f68a3` (embrad/scripts) + the docs pass. Plan: `~/.claude/plans/i-switched-to-planning-zazzy-sloth.md`.

**`/sessions delete` + `/sessions restore` — guided soft deletion with a TTL grace period (branch `wardsondb-optimizations`, 2026-07-16).** Sessions previously could only be closed (a state flip); nothing deleted one. `/sessions delete <name>` is deliberately a conversation, not a keystroke: the slash arm validates (session exists, not `learning` — the sealed identity record is never deletable, not already deleted, a session attached for the flow to run in) then dispatches a kickoff synthetic turn — the intelligence calls `session_summarize` (persisting via `session_summary_save`), presents the summary, and asks the operator WHY. The operator's reply is intercepted (new `pending_session_delete` service state, `AwaitingKickoff → AwaitingReason` two-phase so the kickoff synthetic itself is never mistaken for the reason) and becomes the memorize turn via the resume-briefing enrichment-swap contract (raw reply persists to history; the brain-facing prompt instructs `session_extract` → `remember`/`knowledge_promote`, including one memory recording the deletion and its reason, ending with `[SESSION_DELETE_READY]` — or `[SESSION_DELETE_ABORT]` for a declining reply). The BRAIN executes the deletion post-persistence on the READY marker (the learning-loop `[PHASE_COMPLETE]` precedent), **fail-closed**: abort/no-marker/failed turns never delete, any slash command mid-flow cancels the pending state (announced — deliberately unlike `pending_key_setup`'s no-cancel wart), and execution runs after the history append because `append_message` rewrites the doc from the typed struct and would silently drop a pre-persistence stamp. **Deletion is soft, William's design pick:** `SessionState::Deleted` + serde-additive `deleted_at`/`deleted_reason` meta stamps (turn_count precedent, no schema bump), the stamp patched onto the history + summary docs, and a WardSONDB TTL policy (`deleted_at`, `SESSION_DELETE_RETENTION_DAYS = 7` — module const per the v4 reminders precedent) on all three session collections; the server's TTL worker reaps the docs after the grace period, and docs without the stamp never match its type-bracketed `$lt` cutoff, so live sessions are never at risk. `/sessions restore <name>` undoes it during the grace window (stamps cleared — the null form is equally unreapable; the TTL config stays, harmless without stamps; the session returns Detached). Deleted sessions hide from the `ListSessions` RPC (the mobile sheet renders that list as-is) while the TUI `/sessions` lists them on a trailing grace-period line with the restore hint; `/switch`, `/new` (the name is reserved through the grace period — `create` would append a shadowed second meta doc), and `SessionAttach` all refuse with restore guidance. A boot-time `sweep_reaped_sessions` (beside `ensure_hot_path_indexes`) drops the collection residue once BOTH canonical docs are gone — WardSONDB TTL reaps docs, never collections, and the name-pattern listings would enumerate empty ghosts forever; drop order keeps `.meta` last as the crash-retry key. New `WardsonDbClient::drop_collection` (`DELETE /{collection}`, 404-idempotent), used only by the sweep. Surfaces: brain + console `/help`, web sidebar + param modals, ⌘K/mobile picker rows. No proto change, no schema bump, tool count stays 95, prompts.rs untouched — **zero prompt-cache events**. embra-brain 582 → **593** tests (+11: soft-delete transitions/serde/patch shapes ×5, marker verdict + prompts + phase machine ×4, sweep decision + dotted-name parse ×2). Landed as `b82649b` (feat) + the docs pass. Plan: `~/.claude/plans/i-switched-to-planning-zazzy-sloth.md` (second feature on the branch).

**Duplicate session names refused + shadowed-duplicate healing (branch `wardsondb-optimizations`, 2026-07-17).** William's bulk `/sessions delete` testing surfaced a pre-existing hole: `create()` guarded only COLLECTION existence, so `/new` on an existing name appended a second meta + history doc into the same collections. Every canonical read (`history_query_body` first-doc) shadowed the duplicate — turns kept landing in the original — except the listing, which pushes every meta doc it finds (the visible duplicate rows), and the new soft-delete, which stamps only the canonical docs: deleting a duplicated name left the shadow Active, and after the TTL reap the shadow would become canonical and resurrect the name as an empty live session. Three-part fix: **(1)** `create()` refuses when a meta DOC already exists — doc-level deliberately, so TTL-reaped residue collections (docs gone, awaiting the boot ghost-sweep) stay legitimately reusable by `/new`; best-effort read so a query hiccup can't block creation. **(2)** The `/new` arm pre-checks the meta for tailored messages: live name → "already exists — `/switch` to attach", grace-period name → the existing restore hint (create's own guard stays as defense-in-depth for other callers). **(3)** Boot-time `dedupe_session_docs` (every boot, BEFORE the reaped-session sweep, warn-don't-fail): for each `sessions.*.{meta,history}` collection holding more than one doc, keep the canonical first and delete the shadowed rest — provably unobservable removals (no canonical read ever returned them), looping past the 10-doc query window with a no-progress guard. Ordering matters for the live half-deleted pair from the test: dedupe keeps the stamped-Deleted canonical and drops the shadowed Active dup, so the deletion completes through the reap instead of resurrecting. embra-brain 593 → **594** tests (+1: `shadowed_duplicates` boundaries). No proto/schema/tool-count change; no prompt-cache event. Landed as `0ee0069` (fix) + the docs pass.

**`/stop` — out-of-band operator interrupt for stuck turns (branch `wardsondb-optimizations`, 2026-07-17).** William's local-model testing (Qwen3.6-27b via Ollama/LM Studio) hits reasoning loops that frontier models rarely produce — one unbounded streaming response that `/iter-cap` (a TOOL-iteration cap) never catches, with no way to interrupt it. Two verified architectural facts shaped the fix. First, the operational Converse loop awaits each turn inline, so anything sent on the stream — including a typed `/stop` — parks behind the stuck turn: the stop signal must be out-of-band. Second, dropping the consumer stream did NOT stop generation: all three provider pumps sent fire-and-forget (`let _ = tx.send(...)`) with discarded JoinHandles, draining the SSE stream forever to nobody. The wave: **(1)** an additive unary **`StopTurn` RPC** (brain.proto + apid pass-through — the first proto change since embra-guardian) that bumps a `stop_generation` watch counter while `in_turn` is set; each turn snapshots the counter at start (`StopCheck`), so a stop can only ever affect the turn in flight when it fired — race-free with no reset. **(2)** The **provider pumps now exit on send-error** (anthropic / gemini / openai_compat) — receiver-closed → pump returns → its `reqwest::Response` drops → TCP severs → the local server stops generating. This is simultaneously a standalone leak fix and THE mechanism that makes the abort real; a load-bearing invariant now (never reintroduce a fire-and-forget pump send). **(3)** Abort checkpoints everywhere: `collect_response` and the learning-mode stream consumer `select!` their receive against `StopCheck::triggered()` (wakes even mid-silence, returns a synthetic partial turn), the tool loop checks at the loop top and before each dispatch, and the dispatch await is `select!`-wrapped for mid-tool abandonment (inherits the documented subprocess-leak caveat from the 600 s timeout); no continuation request is ever sent post-stop (no unanswered-tool_use hazard). **(4)** Honest semantics per the refusal precedent: `EarlyStopReason::OperatorStop`, a Warning notice ("Turn stopped by operator — any partial response above is incomplete."), persistence markers (`(response interrupted by operator stop)` on partials, `(turn stopped by operator)` on empties — taking precedence over the tool placeholder), and a synthesized `Done` frame so client streaming animations terminate. **(5)** Surfaces: TUI `/stop` intercepted locally + **Esc-while-busy** (both set a flag the main loop consumes OUTSIDE its `select!`, firing the unary on the same multiplexed channel — the `get_expression` unary-during-stream precedent); desktop web inherits via the PTY + sidebar/palette rows; chat-mobile's Send button becomes a red **■ Stop** while a turn is busy, firing the new **`POST /api/stop`** (the `/api/sessions` fresh-channel pattern — the WS path is deliberately unused since it parks); a brain-side `/stop` slash arm answers the only in-band-reachable case ("No turn in flight"). Learning-mode turns are covered (both learning stream sites ride the same `StopCheck`). Additive proto change; no schema bump, tool count 95, prompts.rs untouched — **zero prompt-cache events**. embra-brain 594 → **599** tests (+5: OperatorStop notice/persistence arms ×3, `StopCheck` snapshot semantics incl. pre-turn-bump invisibility, pump receiver-gone flag). Landed as `eae02cc` (brain/proto/apid) + `1a0d6da` (console/web/web-ui) + the docs pass. Plan: `~/.claude/plans/i-switched-to-planning-zazzy-sloth.md` (third feature on the branch).

**WardSONDB vendored into the workspace as `crates/wardsondb` (2026-07-17).** The 12th workspace member: a byte-faithful import of the upstream tracked tree at `Ward-Software-Defined-Systems/WardSONDB@42fea43` (its `main`, same-day tip — the code-review campaign fully merged), minus upstream's `Cargo.lock` (the workspace lock governs) and `CONTRIBUTING.md` (embraOS's issue-only policy governs) — 55 files, ~12.6k LoC of source plus tests/benches/API docs; a blob-SHA diff against upstream proved the import byte-identical. Deliberate fork: upstream continues as its own product; this copy diverges from here (engine feature-gating for fjall-only builds is the queued first divergence candidate). Zero Rust source edits — crate version stays `0.1.0` (apid's hardcoded `wardsondb_version` and Buildroot's `WARDSONDB_VERSION` stay truthful) and the runtime contract is untouched (embrad still launches `/usr/bin/wardsondb --storage-engine <baked> --port 8090 --data-dir /embra/data/wardsondb …`; `buildroot/package/wardsondb/wardsondb.mk` unchanged — its `_SITE` already pointed at the shared `target/<triple>/release/`). Storage-critical deps resolved to exactly upstream's regression-tested lock pins on first resolution — fjall 2.11.2 / lsm-tree 2.10.4 / value-log 1.9.0 / rust-rocksdb 0.46.0 / rust-librocksdb-sys 0.42.0+10.10.1 / roaring 0.10.12 / tikv-jemallocator 0.6.1 — no `--precise` surgery needed; the pins carry on-disk-format compat with existing DATA partitions and backups (the temp-dir test suite structurally can't); shared deps (tokio/axum/rustls/…) stay at embraOS's existing lock pins (only new-major coexistence: wardsondb's `thiserror 2` beside the workspace's `1`). `scripts/build-image.sh` + `build-image-aarch64.sh` lose Step 2 (the sibling-repo build/copy; `WARDSONDB_DIR` is gone) — wardsondb now rides Step 1's workspace build; later step labels deliberately unchanged. Docs: QUICK-START / AARCH64-BUILD / INTEL-MAC-BUILD drop the sibling-clone steps and reframe the bindgen/zstd-sys troubleshooting as in-tree; README's persistence sentence notes the in-tree fork. Workspace 11 → 12 members; the workspace suite gains wardsondb's 224 tests (37 unit + 184 integration + 3 deadlock-stress; the unit tests execute in both lib and bin targets → 261 executions) — full `cargo test --workspace` measured **11 s wall** warm (the integration servers are tempdir + ephemeral-port and heavily parallel), so `--exclude wardsondb` is a compile-time escape hatch, not a run-time one; the first cold `cargo check --workspace` pays the native chain (rocksdb C++ / zstd / bindgen / jemalloc) once — 2 m 56 s wall / 29 m CPU measured. Vendored `crates/wardsondb/CHANGELOG.md` is frozen upstream history @ `42fea43`; future in-tree WardSONDB changes log here. Landed as `3dc3e29` (vendor + member + lock, 57 files / +28,719) + `8417054` (build scripts) + the docs pass. Plan: `~/.claude/plans/wardsondb-just-completed-a-elegant-llama.md` (fourth feature on the branch).

**GitHub Actions CI — first CI for the repo (`1ca8ab9`→`d631eff`, 2026-07-18).** William-directed as the pre-merge gate for the vendoring line: a Rust-level regression net that complements — never replaces — the local `build-image.sh` → Buildroot → QEMU pipeline (image/runtime validation stays the build hosts' authority; the standing local-testing discipline is unchanged). Four jobs on push/PR/dispatch (`.github/workflows/ci.yml`; per-ref concurrency, `contents: read`, toolchain pinned `1.94.1` workflow-only, every cargo invocation `--locked`): **guards** — the seven storage-critical `Cargo.lock` pins must match the vendored WardSONDB's regression-tested set, mechanizing the vendored-fork invariant (a Dependabot or casual `cargo update` bump goes red instead of sliding through); **frontend** — Trunk (pinned `0.21.14`, matching the dev host) builds `embra-web-ui` → WASM exactly like build-image.sh Step 0.5 and hands `dist/` to the compile jobs by artifact, putting the frontend under CI for the first time; **test** — `cargo test --workspace` plus the wardsondb lint ratchet (`clippy -p wardsondb --all-targets -- -D warnings`; the crate is zero-warning at vendor time and the ratchet keeps the fork that way); **musl-cross** — the ship-shape static cross-build with an 8-binary existence assert. Three latent facts surfaced by runs 1–2 and fixed in-wave: `embra-web` cannot compile on a fresh checkout (rust-embed's `#[folder]` is a compile-time dependency on gitignored Trunk output — invisible locally because every dev/build host has run Step 0.5; hence the frontend job), **musl.cc drops connections from GitHub's IP space outright** (5/5 connect-timeouts while the same URL serves fine elsewhere — retries can't fix an IP-level block, so CI uses Bootlin's Buildroot-built musl toolchain, `x86-64--musl--stable-2025.08-1` / gcc 14.3.0, sha256-pinned + cached + relocate-sdk-handled, locally smoke-tested to produce a working static-pie wardsondb before wiring; exact musl.cc parity stays a build-host property — an org-hosted toolchain mirror is the open follow-up if exact-parity CI is ever wanted), and dtolnay's minimal toolchain profile lacks clippy (`components: clippy`). Run 3 = first all-green: guards 5 s / frontend 41 s (warm) / test 10 m 27 s / musl-cross 15 m 53 s (cold); rust-cache keys then rotated (`d631eff`) because the failed early runs had claimed them with partial saves. Steady state once seeded (run 5, all caches warm): **~3 min wall for the whole pipeline** — guards 6 s / frontend 39 s / test 2 m 03 s / musl-cross 2 m 29 s. The aarch64 job is queued until the x86_64 pipeline is proven — which run 3 did; adding it is William's call. Same-day follow-up (`d4f338b`, from William's warning report): every first-party action bumped to its Node-24 major — checkout v7 / cache v6 / upload-artifact v7 / download-artifact v8, each runtime verified per-tag against its `action.yml` via the API before wiring (the artifact actions' v5/v6 still declare node20) — killing the Node-20 deprecation annotations (4 → 0, run-verified; `Swatinem/rust-cache@v2` already floats to node24, `dtolnay/rust-toolchain` is composite); a `dist/index.html` assert after each artifact download guards future layout changes from silently embedding an empty frontend (rust-embed only errors on a *missing* folder); and `.github/dependabot.yml` enables version updates for the **github-actions ecosystem only** (monthly, grouped single PR, CI-validated) — cargo deliberately unconfigured, the storage pins stay manual-and-guarded. No Rust source change; zero prompt-cache events.

**Build-script fd preflight + `JOBS` parity (branch `wardsondb-optimizations`, 2026-07-19).** William's aarch64 rebuild rounds fail the OrbStack Docker Buildroot pass with varying errors consistently including `Too many open files` — now on BOTH Apple-Silicon Macs (the M1's 2026-07-18 pass didn't hold) — while the Intel-Mac x86_64 flow passes; neither `--ulimit nofile=1048576:1048576` nor a named volume for `buildroot-src` cured it, and at least one failure followed a fresh OrbStack restart. Session diagnosis: the Buildroot phase is **byte-identical pre/post the WardSONDB vendoring** (`8417054` only removed Step 2, which sits inside the region `--buildroot-only` never executes), so the vendoring correlation is environmental. Leading theory — the known OrbStack class ([orbstack#2255](https://github.com/orbstack/orbstack/issues/2255), [orbstack#1253](https://github.com/orbstack/orbstack/issues/1253)) where the macOS-side file provider holds an fd per accessed inode on bind mounts, bounded by `kern.maxfilesperproc` (~138k default); a cold Buildroot tree touches 300k+ inodes, and the resulting EMFILE propagates into the guest through virtiofs — a channel no in-container `--ulimit` can reach (which is why Rung 1 failed). The named volume DOES cover the in-tree kernel-compile dir (`buildroot-src/output/build/linux-*/`), making Rung 2's uncaptured failure the key open question (ENOSPC — a cold volume rebuild is ~60 GB inside the OrbStack VM while the shadowed Mac-side `buildroot-src/` still holds ~60 GB — and OOM without `-e JOBS=2` on the 8 GB M2 are live alternates). A machine-side diagnostic runbook (parity snapshot across all three Macs with the Intel as passing control, a minutes-scale synthetic reproducer measuring the provider fd ceiling, a `sysctl kern.maxfilesperproc` mitigation probe, and one instrumented named-volume build with the bind-vs-volume error-path discriminator) is with William; guide troubleshooting sections deliberately wait for its verdict. Landed now (guest-side share + observability): **(1)** both image scripts gain an **fd preflight** before Step 4 — the soft fd limit self-raised to the hard cap (OrbStack containers default soft 20480 / hard 1 M, so the docker `--ulimit` flag becomes redundant for guest-side limits) plus a one-line `fd limits: soft/hard; system-wide` report that lands in every future build log; **(2)** `build-image.sh` now honors **`JOBS`** like the aarch64 script always did (`make -j"$JOBS"`, default unchanged at all cores) — INTEL-MAC-BUILD.md's documented `-e JOBS=2` OOM workaround was a silent no-op against the x86 script; **(3)** a parallelism echo (`-jN` + override hint) in both. Shell-only; no Rust, no proto/schema/tool-count change, zero prompt-cache events. Landed as `5d06e58` (fix) + the docs pass. Plan + runbook: `~/.claude/plans/post-integrating-wardsondb-as-hashed-candle.md`. **Same-day follow-up (`bbc7271`):** the M2's first runbook Phase-3 run exposed a named-volume trap — with a volume mounted at `buildroot-src` the mountpoint directory always exists, so the `! -d` clone guard never fired and the EMPTY volume fell into the version check, where git's not-a-repository failure was misread as "`buildroot-src` has local changes". This also **reinterprets the earlier Rung-2 data point**: that run almost certainly died in this guard and never reached a kernel compile — every real EMFILE observed so far had the tree on the bind mount, and the named-volume flow gets its first genuine test only now. Both scripts now clone when `BUILDROOT_DIR` is missing OR empty (git clones into an existing empty dir; mechanism verified), and the version check fails honestly (`not a git checkout`) on non-empty non-git content.

**Named-volume Buildroot flow — the Mac Docker builds default to a VM-native `buildroot-src` volume (branch `wardsondb-optimizations`, 2026-07-19).** The runbook verdict, same day: William's populated Phase-3 run on the 8 GB M2 — the machine that failed every bind-mount variant — completed a full cold Buildroot build with the tree in a Docker named volume, while every genuinely observed EMFILE had the tree on the `-v "$PWD":/work` bind mount. Root cause (orbstack#2255/#1253 + the phpstan#14307 VirtioFS analysis): the Docker runtime's macOS-side file provider holds an fd per accessed inode in one host process capped by `kern.maxfilesperproc` (~138k default); a cold Buildroot tree touches 300k+ inodes (kernel source alone ~87k), so a single bind-mount build exhausts the pool and the EMFILE propagates into the container through the file-sharing layer — unreachable by any in-container `--ulimit`, which is why that rung never helped. William's call: the named volume becomes the **documented default for BOTH Mac flows** — `embraos-br-aarch64` / `embraos-br-x86_64` (per-arch names; the scripts share the literal `buildroot-src` dir) in every `docker run` across AARCH64-BUILD.md + INTEL-MAC-BUILD.md (Quick Start, `JOBS` variants, backup-workflow rebuild, OOM-resume), both scripts' header + Step-4-guard help text matched. New validated troubleshooting: a Step-4 `Too many open files` entry in the aarch64 guide (symptom / host-side mechanism / the volume fix / `sudo sysctl kern.maxfiles* ` bind-mount stopgap) with a compact cross-referencing note in the Intel guide, and the **stale-tree QEMU warning** in both — the run scripts prefer `buildroot-src/output/images/` over the `output/images/` Step 5 populates, so a leftover bind-mount-era Mac-side tree shadows freshly built images until deleted (`rm -rf buildroot-src`, also returning ~60 GB); fresh clones never grow a Mac-side tree, so it is one-time migration cleanup. Banner notes added to both guides pending full-checklist re-stamps (M1 + Intel-Mac replays are William's next round). Docs + script help text only — no behavior change beyond the `bbc7271` empty-dir clone above; zero prompt-cache events. One docs-wave commit (both guides, both scripts' help text, this entry).

The `embra-web` and `embra-guardian-v1` feature branches were retired (deleted local + origin) 2026-05-18 once merged. `embra-web-mobile` was fast-forward-merged 2026-05-28 (`e04374a`); its three follow-up bugs (#1 / #2 / #3) are resolved on **embra-brain-broadcaster**, fast-forward-merged to `main` and pushed to `origin/main` 2026-05-29 (`7f6c479`); that branch is retired (deleted local; never pushed to origin). `embra-desktop` (the separate GUI workstream, `3588236`, forked from `69200e9`; 27 commits, never merged) was deleted local + origin as a relic on 2026-07-03 at William's direction — its history survives in the `embra-desktop.ops.wsds` clone. `knowledge-and-webui-fix` (the 2026-07 memory/KG line — five follow-up waves, all merged) was closed out 2026-07-05: `main` and the branch identical at `fade54a`, deleted local + origin. `llm-provider-optimizations` (four waves, 2026-07-06 → 2026-07-08: Fable 5 + Opus 4.8 default + refusal-as-error + `/effort` + `set_name` + expression-panel scroll + Anthropic transient-error retry + the cross-provider session-block message fix — all merged) was closed out 2026-07-08: `main` and the branch identical at `a8a8d02`, deleted local + origin; remaining live verification (replicating a 529 overload episode) rides `main`, further findings deferred to the upcoming code review. `session-ux-fixes` (2026-07-11 — the session-UX fix trio + the two `/sessions` listing follow-ups, all merged) was closed out the same day: operator-tested, fast-forward-merged to `main` @ `7e4eae4`, branch deleted (local only — it was never pushed to origin). **`wardsondb-optimizations`** opened 2026-07-16 and is the **live** working branch — it carries the WardSONDB code-review compatibility wave, the `/sessions delete`/`restore` feature (operator-tested at scale: a 42-session bulk cleanup), the duplicate-session-name fix, the `/stop` operator interrupt, the WardSONDB vendoring, and the repo's first GitHub Actions CI above. Pushed + fast-forward-merged to `main` 2026-07-17 (the WardSONDB `code-review` branch merged to its own main the same day, so both sides of the compatibility wave are mainline); the **WardSONDB vendoring and the first GitHub Actions CI** were then fast-forward-merged to `main` 2026-07-18 — the branch's second FF, with branch-tip CI green (~3 min pipeline) at the merge point. The branch **stayed live** through the aarch64 build-environment investigation, **resolved 2026-07-19**: the Docker Buildroot pass had regressed to file-descriptor exhaustion on both Apple-Silicon Macs (bind-mount Buildroot trees exhaust the macOS file provider — the named-volume entry above carries the mechanism), the volume flow is operator-verified on the previously always-failing M2, and both Mac guides now default to it. Third fast-forward to `main` 2026-07-19 at the docs-wave tip. William's next round: replay on the M1 and the Intel Mac under the new default flow.

---

## v0.5.0-phase1 — 2026-05-07 (`69200e9`)

**Phase 1 declared stable. Sprint 5 OPENAI-COMPAT-PROVIDER-01 (Ollama + LM Studio) + REASONING-STREAM-01 follow-up.**

Adds Ollama and LM Studio as additional `LlmProvider` implementations via a single `OpenAICompatProvider` with `OpenAiCompatPreset::{Ollama, LmStudio}` discriminator. Schema v11 (idempotent `openai_compat` field stamp on `config.system:config`); STATE bearer plumbing at `/embra/state/bearer_<preset>` mode 0600; 4-way wizard with Endpoint → Bearer → Probe-and-Select sub-flow in `crates/embra-brain/src/setup/wizard.rs`. Defensive multi-key reasoning parser; `Block::ProviderOpaque{kind:"reasoning"}` CoT round-trip per OpenAI cookbook preserve/drop rules; harmony-token sanitization always-on. `/provider --setup <ollama|lm_studio>` 4-step post-wizard reconfigure with bearer hot-reload via env var. Model-aware `reasoning_effort` gating via `model_supports_reasoning_effort` heuristic.

REASONING-STREAM-01 follow-up (added 2026-05-06): streams provider reasoning / chain-of-thought deltas to the existing 6-row expression panel as they arrive. Anthropic via `display: "summarized"` (was `"omitted"`), Gemini via new `includeThoughts: true` on `thinking_config`, OpenAI-compat via existing `delta.reasoning` / `delta.reasoning_content` paths now also emitting `StreamEvent::ReasoningDelta`. Load-bearing privacy contract: `ReasoningDelta` MUST NOT enter `full_response` / `accum_text` / session history / IR round-trip Text blocks; provider signatures stay on `Block::ProviderOpaque` exactly as before. Default-on with opt-out via `SystemConfig.show_reasoning: Option<bool>` + `/show-reasoning <on|off>`. Operator-verified live across all four providers 2026-05-06.

90 ToolDescriptors unchanged. Operator-driven post-Stage-6 fix wave (8 commits 2026-05-04) covered live-smoke surprises: bearer step yes/no Selector, `tool_choice: "auto"` string form (LM Studio rejects Anthropic's object form), `properties: {}` stamp for parameterless schemas (LM Studio's zod validator rejects missing field), provider-aware operational-mode entry precheck, `reasoning_effort` omission for non-reasoning models. `docs/RECOMMENDED-MODELS.md` shipped as operator-facing starter doc with per-family reasoning-control matrix.

Full detail: see ARCHITECTURE.md (grep `Phase 1 Sprint 5 — OPENAI-COMPAT-PROVIDER-01` and `Phase 1 Sprint 5 follow-up — REASONING-STREAM-01`).

---

## v0.4.0-phase1 — 2026-04-25 (`c57d9cf`)

**Sprint 4 — GEMINI-PROVIDER-01: Pluggable LLM Provider + Gemini 3.1 Pro.**

The spec defines a provider abstraction (`LlmProvider` trait + neutral IR) so `embra-brain` can swap the Anthropic backend for Google's Gemini 3.1 Pro without touching the loop driver, prompts, sessions, or tools. Stages 1–10: provider trait + neutral IR; Anthropic refactor (zero behavior change); Gemini wire types + tool schema translator (UPPERCASE types, `$ref`/`definitions` inliner, `anyOf` supported, `oneOf`/`allOf` rejected); Gemini SSE streaming parser; HTTP driver + `LlmProvider` impl; Context Cache lifecycle (simplified — drop TTL-refresh-mid-turn, drop 4096-token threshold, 4-event telemetry); wizard provider step; `/provider` slash command + cross-provider session block (hard refuse via `SystemMessage::Error`); WardSONDB schema migration v9; supervisor wiring + status line.

Operator directives brought in-sprint: **D8** (`EMBRA_GEMINI_MODEL` override + per-turn `gemini::diag` telemetry) and **D2** (per-provider API keys, schema v10, `/provider --setup` multi-turn runtime key add). Six-commit post-smoke fix chain on `main` 2026-04-24/25: per-call key resolution (`/provider` swap was sending stale boot-time key), workspace 0.2 → 0.4 + `CARGO_PKG_VERSION` propagation, learning `CATEGORY_COUNTS` 85 → 90, `/help` refresh, `ModeTransition` emit on `/provider` swap, `Brain:` token in wizard+learning transitions, `SessionManager::create` stamps `meta.provider`, server-stale `cachedContent` 403/404 recovery. Post-merge hotfix `5470b37` (2026-04-26) repairs the cross-provider guard; post-merge fix `ebf8fec` (2026-04-28) replaces the silent `MAX_TOOL_ITERATIONS` break with a graceful capping sequence (synthetic `tool_result` for every undispatched call + final summary + `SystemMessage::Warning`); cap raised 10 → 100 per operator testing; adds `/iter-cap` runtime knob.

Also landed in this window: **PR #3 "Ubuntu 26.04 build"** (`2e66077`, 2026-05-02) — Buildroot 2024.02 → 2026.02.1 LTS bump for Ubuntu 26.04 host compat (GCC 15 `-std=gnu23`, libxcrypt split from glibc, M4-gnulib `[[nodiscard]]`, CMake 4.x).

90 ToolDescriptors (unchanged). 211 workspace tests (was 142 at Sprint 3 close).

Full detail: see ARCHITECTURE.md (grep `Phase 1 Sprint 4 — GEMINI-PROVIDER-01`).

---

## v0.3.0-phase1 — 2026-04-24 (`da469a6`, PR #2 merge)

**Sprint 3 — NATIVE-TOOLS-01: Native Tool-Use Migration + four post-merge passes.**

Migration from literal-tag `[TOOL:name args]` text dispatch to the Anthropic Messages API's native tool-use surface. Text channel and tool channel are now separated at the API layer via structured `tool_use` / `tool_result` content blocks; the FIX-01 cross-turn echo-via-context failure class is structurally impossible post-migration. Workspace grows 7 → 9 crates with two new members: `embra-tools-core` (shared `DispatchError` / `BoxFut` / `JsonValue`) and `embra-tool-macro` (proc-macro attribute `#[embra_tool(name, description, is_side_effectful?)]`). New tool registry hub at `crates/embra-brain/src/tools/registry.rs` with `inventory::collect!(ToolDescriptor)` autoregistration and a 2 MiB result cap.

Earlier Sprint 3 work also bundled into this release: WardSONDB pluggable storage engine (rocksdb / fjall, locked into DATA on first boot via `.engine` marker); EXPR-01 expression panel; tool-bug fix series (13 commits); FIX-01 residual diagnostic; tool coverage expansion (7 commits); unknown-tool dispatcher fix; post-verification bug triage.

Four post-merge passes since Sprint 3 PR #1 (`fcb8035`, 2026-04-22): 2026-04-23 tool-fix pass (9 Embra_Debug issues closed); 2026-04-24 pass #1 (arch-strip + #44/#45/#46/#49); pass #2 (#52/#53/#54/#55); pass #3 (#56/#57). 15 Embra_Debug issues closed in total. 90 ToolDescriptors (post-NATIVE-TOOLS-01 + tool-fix-pass additions). 142 workspace tests. QEMU E2E smoke verified across all three post-verification passes.

Full detail: see ARCHITECTURE.md (grep `Phase 1 Sprint 3 — NATIVE-TOOLS-01` and the four Sprint 3 post-merge pass markers).

---

## v0.2.0-phase1 — `phase1-arch-rework` pre-merge (`dda8c6c`)

**Phase 1 complete — Sprint 2 cross-session knowledge graph + post-sprint polish.**

Cross-session knowledge graph split across three WardSONDB collections plus an edge layer: `memory.entries` (episodic turns), `memory.semantic` (promoted facts/preferences/decisions/observations/patterns), `memory.procedural` (promoted structured procedures), and `memory.edges` (typed, weighted, directed edges with auto-derived `same_session` / `temporal` / `tag_overlap` and brain-created `enables` / `contradicts` / `refines` / `depends_on` / `derived_from`). Tools with prefix `knowledge_`: `promote`, `link`, `unlink_edge`, `unlink_node`, `update`, `traverse`, `query`, `graph_stats`. Auto-enrichment wraps the in-flight user message in a `<retrieved_context>` block when retrieval yields ≥1 result scoring ≥0.3. Post-sprint polish: auto-enrichment refinements, config-wizard validation, Opus 4.7 upgrade, empty-assistant guards.

Full detail: see ARCHITECTURE.md (grep `Phase 1 Sprint 2 — Cross-Session Knowledge Graph` and the preceding `Phase 1 Sprint 1` bug-fix pass).

---

## v0.1.0-phase0 — Phase 0 Sprint 5 Complete (`78a4f86`)

**Phase 0 proof-of-concept (Docker-containerized).**

Phase 0 implementation was a Docker-containerized proof-of-concept running `embrad` as the main process with WardSONDB as a child process. Single-container architecture; `embra-brain` not yet split out as a separate service; no inter-process gRPC; conversational terminal directly in the embrad process. Configuration via Config Wizard at first run, six-phase Learning Mode to seal the soul (serialized with `serde_json::to_string_pretty` and SHA-256-hashed), then a persistent conversational terminal with a brain backed by the Anthropic API.

WardSONDB collections established at Phase 0: `soul.invariant`, `config.system`, `memory.identity` / `memory.user` / `memory.entries`, `sessions.*.meta` / `history` / `summary`, `tools.registry`, `knowledge.definitions`, plus operational collections (`drafts`, `reminders`, `plans`, `tasks`, `crons`, `system.migrations`, `system.consolidation_log`).

Full detail: see ARCHITECTURE.md `🔵 Phase 0 Architecture` (starts line 1463).

---

## Initial commit — Phase 0 founding (`330631e`)

embraOS founding commit — initial continuity-preserving AI operating system Phase 0 implementation. Derived from the v3 design document (`embra.ai.agent Design v3`, 2026-03-13).
