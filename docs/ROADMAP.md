# Roadmap

embraOS delivery status by phase. Project landing page: [../README.md](../README.md).

| Phase | Description | Status |
|---|---|---|
| **Phase 0** | Proof of concept — Docker container, Anthropic API, core UX | ✅ **Stable** |
| **Phase 0 — Sprint 1** | Bug fixes (9+1 crash), design improvements (4), new tool categories (security, engineering) | ✅ **Complete** |
| **Phase 0 — Sprint 2** | Bug fixes (3), expanded git/GitHub toolset (12 new tools), enhanced port scanner, embraCRON scheduling | ✅ **Complete** |
| **Phase 0 — Sprint 3** | Session access tools (5), memory consolidation (2), session consolidation (3), schema migration framework | ✅ **Complete** |
| **Phase 0 — Sprint 4** | SSH remote admin (4 tools), tag filter fix, timezone-aware timestamps, `/copy` deferred | ✅ **Complete** |
| **Phase 0 — Sprint 5** | SSH ControlMaster refactor, Brain API upgrade (128K output, adaptive thinking, 1M context), WardSONDB integration upgrades, new filesystem/git tools (file_delete, file_move, dir_delete, git_rm, git_mv) | ✅ **Complete** |
| **Phase 1 — Initial Sprint** | Core OS — QEMU-bootable image, immutable SquashFS rootfs, full boot chain (embra-init → embrad → services), config wizard, Learning Mode, soul sealing, gRPC architecture, serial TUI | ✅ **Complete** |
| **Phase 1 — Sprint 1** | Bug fixes & UX — tool feedback loop, timezone display, multi-line input, git/SSH/GitHub setup commands, input word-wrap, tool output truncation, Unicode crash fix | ✅ **Complete** |
| **Phase 1 — Sprint 2** | Cross-session knowledge graph — semantic/procedural promotion, typed/weighted edges, BFS traversal, graph-aware retrieval, 6 KG tools, `/feedback-loop` command | ✅ **Complete** |
| **Phase 1 — Sprint 3** | WardSONDB pluggable storage engine (`--storage-engine <fjall\|rocksdb>`), EXPR-01 expression panel, NATIVE-TOOLS-01 Anthropic native tool-use migration, tool-coverage expansion, four post-merge fix passes closing 15 Embra_Debug issues (90 tools, 142 tests) | ✅ **Complete** |
| **Phase 1 — Sprint 4** | GEMINI-PROVIDER-01 — pluggable LLM provider abstraction, Anthropic + Google Gemini 3.1 Pro backends, neutral IR loop driver, Gemini explicit context cache lifecycle, per-provider API keys (schema v10), `/provider [status\|<kind>\|--setup]` slash command, wizard provider step + post-merge cross-provider guard hotfix + Embra_Debug #80 graceful tool-iteration cap with `/iter-cap` runtime knob (90 tools, 234 tests) | ✅ **Complete** |
| **Phase 1 — Sprint 5** | OPENAI-COMPAT-PROVIDER-01 — Ollama + LM Studio via single `OpenAICompatProvider` with preset discriminator, schema v11 + STATE bearer plumbing, 4-way wizard with Endpoint → Bearer → Probe-and-Select sub-flow, `/provider --setup <ollama\|lm_studio>` runtime reconfigure, model-aware `reasoning_effort` gating, plus REASONING-STREAM-01 follow-up — live provider reasoning streamed to the expression panel, default-on with `/show-reasoning <on\|off>` opt-out (90 tools, 421 workspace tests) | ✅ **Complete** |
| **soul/identity rendering** | Post-Sprint-5 increment (2026-05-15, untagged FF) — structured + precedence soul/identity/profile rendering: new read-time renderers `brain/{soul_render,identity_render,user_render}.rs` (never rewrite the stored doc; SHA-256 seal/verify path unchanged), `operational_mode()` rewrite with precedence + conflict-resolution + self-check, Phase 1–3 learning prompts emit typed schemas, KG mechanics relocated into the cached tool descriptions; deterministic (prompt-cache stable), sealed-soul core + embra-trustd untouched. | ✅ Merged to `main` 2026-05-15 (FF) |
| **embra-web console** | Post-Sprint-5 increment (2026-05-16, untagged FF) — self-hosted HTTPS console on `:3345` (Axum 0.8 + tokio-rustls) that spawns the real `embra-console` serial TUI as a `portable-pty` child and bridges it to the browser as an xterm.js pane over a `/ws/terminal` WebSocket; 2 crates (`embra-web` workspace member + workspace-excluded `embra-web-ui` Leptos 0.7/WASM, Trunk-built, `rust-embed`-embedded); `arbiter.rs` single-writer + observers + explicit takeover; per-command parameter modals + guided provider-setup; `/ml` multi-line editor modal + `EMBRA_WEB_PTY`-gated bracketed paste (`cf84131`). Now the **default UI** (kernel `embra.web=1`); serial TUI (`EMBRA_TUI=1`) is the stable fallback. Sits entirely above apid — no proto / WardSONDB-schema / registry / tool-count change. | ✅ Merged to `main` 2026-05-16 (FF); branch retired 2026-05-18 — default UI, **stable** (promoted 2026-07-05) |
| **`embra-guardian` v1** | Dynamic-tool substrate (pulled forward from Phase 2) — operator-authored dynamic tools: `syn` static validation + denylist, in-OS prebaked Rust toolchain → `wasm32`, `wasmtime` sandbox (epoch + memory cap, instance-per-call), capability-broker host imports (`http_get`, Brave `web_search`; zero ambient guest authority), `guardian_call`/`guardian_list` meta-tool gateway (prompt-cache-stable), WardSONDB persistence + boot reconcile, new `embra-guardian` crate (92 tools, schema v12). R1 gate + full workspace tests + QEMU operator-test green; ~+90–130 MB rootfs signed off | 🔬 Merged to `main` 2026-05-17 (FF); branch retired 2026-05-18 — experimental |
| **Post-Sprint-5 increments** | Waves FF-merged to `main` between the embra-guardian v1 merge and Sprint 6 (releases `v0.6.0-phase1` → `v0.10.0-phase1`) — guardian self-authoring + the soul-spec replicant check (`guardian_propose`, → 93); `knowledge_dump` + the `guardian_call` `data_file` bridge (→ 94); the memory/KG search-window fixes + the arm-split retrieval perf rework (`knowledge_query` 5–8 min → ~2 s); `/stop`; `/sessions delete\|restore`, the duplicate-name fix, turn counts + the resume-briefing gate; Fable 5 + Opus 4.8 + `/effort` + refusal honesty + transient-error retry; `set_name` (→ 95); WardSONDB vendored in-tree as `crates/wardsondb` + the first CI; the named-volume Mac Buildroot flow; the KG-native IDENTITY+SOUL graph + Learning-Mode import (schema v13); KG hygiene (`knowledge_audit`/`knowledge_merge`, → 97), the type-partitioned traversal hop + seed packs, `system_logs` (→ 98); the Opus 5 line-up refresh | ✅ **Complete** — chronicle in [CHANGE-LOG.md](CHANGE-LOG.md) |
| **Phase 1 — Sprint 6** | Final Phase 1 sprint (branch `phase1-sprint6`, 2026-08-04 → 2026-09-17; releases `v0.11.0-phase1` → `v0.14.0-phase1`) — session-transcript fidelity (full-content `session_read`/`session_delta` under a 64 KiB paging budget, every-segment turn persistence); self-hosted git servers (operator CA trust via `seed-state.sh --ca-dir`, `/git-token`, the `gl_*` GitLab set 98 → 102, the `embraos-git` + `embraos-core` seed packs); macOS disk-image seeding (`seed-state-mac.sh`, `--wipe`); host-side file ops + media — `file_patch` (→ 103), the file-tool caps wave, image input on every surface + `image_view` (→ 104), `image_generate` on Gemini image models + `/image-provider` (→ 105), `file_copy` (→ 106); the web-console wave (repaint on browser attach, eight collapsible sidebar groups + filter); the GitLab migration (primary origin + CI on the self-hosted runner, GitHub kept as the published mirror; `gl_*` at gh_* parity → 116); KG retrieval waves A + B (graph expansion deleted, IDF weighting, in-OS embeddings via tract + `bge-small-en-v1.5`, `/embeddings`); system-prompt neutrality for imported identities; the close-out wave (console keys, backup `--image`, https endpoints keep 443, per-provider `/effort`, provider endpoint health + the web `provider` pill, web layout, embrad burst restart budget). Schema stays v13 (116 tools, brain 995 tests) | ✅ **Complete** — closed 2026-09; close-out E2E findings routed to the code review |
| **Pit Stop** | Code review branch — security audit, AI slop cleanup, refactoring | Planned |
| **Phase 2** | Terminal & Sessions — Full TUI rewrite; API Web Searches (the `embra-guardian` v1 dynamic-tool substrate + brokered web search with prompt-injection protection was pulled forward — see the `embra-guardian` v1 row above, now merged to `main`) | Planned |
| **Phase 3** | Module System — `embra-guardian` v2, `embractl` management CLI (the `talosctl` equivalent), `embra-brain` Local/Hybrid option via external Ollama but default/recommended remains Anthropic API, LLM-driven Continuity Engine feedback loop (local/API/Hybrid options), MCP server modules via `embra-guardian` governance proxy, containerd runtime, governed capability expansion | Planned |
| **Phase 4** | Image Factory — GPT Partition Alignment, additional bootable ISO builds, bare metal and Kubernetes deployment | Planned |
| **Phase 5** | Sovereign Intelligence Options, OS Updates, and Security — A/B partition scheme with automatic rollback, LUKS encryption, mTLS enforcement, custom kernel, custom embraOS-QNM AI model option, local LLM inference/offline operation, zero external dependencies | Planned |
