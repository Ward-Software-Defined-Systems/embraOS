//! embra-apid — gRPC + REST API gateway for embraOS.

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    tracing_subscriber::fmt::init();
    tracing::info!("embra-apid starting");

    // TODO: Implement in Doc 04
    tracing::error!("embra-apid not yet implemented");
    Ok(())
}
