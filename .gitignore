# Build artifacts
target/
*.swp
*.swo
*~

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Environment
.env
*.pem
*.key

# Internal working documents (Claude Code instructions & architecture research)
.claude/
CLAUDE.md
ARCHITECTURE.md
embraOS-Phase1-Implementation/
embraOS-Phase4-Implementation/
EXPR-*-*.md

# Docker
*.tar

# Data / runtime
data/
*.db
*.log

# Build outputs (disk images, initramfs, Buildroot)
output/
initramfs.cpio.gz
buildroot-src/

# embra-web frontend bundle — Trunk/WASM output, regenerated by
# build-image.sh Step 0.5 and embedded into embra-web via rust-embed.
crates/embra-web-ui/dist/

# Cargo (linker config is per-machine, lives in ~/.cargo/config.toml)
.cargo/

# Claude Code
.claude/
embraos-tools-instructions.md
embraOS_Phase0_Sprint1.md
ARCHITECTURE_Phase0_Sprint1.md

# Phase 0 reference (retired from branch, kept locally)
# Leading slash anchors to repo root so crates/*/src/ are not matched.
/src/
/Dockerfile
/Cargo.toml.phase0
